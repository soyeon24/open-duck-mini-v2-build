# 시뮬레이션 / 기립(standup) 작업 노트

> 이 파일은 시뮬·외형·하드웨어 담당 세션이 관리합니다.
> `ubai/`, `NEXT_STEPS.md`, `from_ubai/` 는 UBAI 담당 세션이 만든 것이니 지우지 마세요.
> (한 번 `ubai/` 를 지웠다가 복구한 적 있음)

---

## 다시 시작할 때 (2026-09-08 갱신)

**클러스터에서 `alive` 가중치 스윕 3개(948600/948601/948602)가 돌고 있습니다.**
2026-09-08 제출, 제출 시점엔 `PD (Priority)` 대기 상태였습니다. 노트북과 무관하게 돕니다.

| 잡 | ALIVE_W | 출력 디렉토리 |
|---|---|---|
| 948600 | 10.0 | `checkpoints_alive10` |
| 948601 | 5.0 | `checkpoints_alive5` |
| 948602 | 2.0 | `checkpoints_alive2` |
| (hw8, 회수 완료) | 20.0 = 대조군 | `checkpoints_hw8` |

셋 다 `HEAD_POS_W=-8.0`, 300M, `WARM=none`, 나머지 설정은 hw8 과 동일합니다.

⚠️ **아직 안 한 것: `-8.0`(hw8) 의 보행 품질 뷰어 확인.** 정지 자세는 hw8 이 압승인데
걷는 모습은 여태 눈으로 안 봤습니다. 스윕 결과가 오기 전에 집에서 이걸 먼저 하세요.
(2026-09-09 에 hw8 을 헤드리스로 30초 걸려 봤을 때 넘어지진 않았습니다 — 아래 토크
측정 항목 참고. 다만 그건 넘어짐 여부만 본 것이고 걸음걸이 품질은 여전히 눈으로 봐야 합니다.)

> (이전 기록: 2026-09-06 에는 큐가 비어 있었음 / 09-04 걷기+머리추종 / 08-31 기립 v3)

```bash
ssh <YOUR_ID>@<GATE1_IP> -i <KEY_DIR>\ubai-<YOUR_ID>.pem
```
- ⚠️ **학교 네트워크에서만 접속됩니다** (사설 IP, VPN 없음). 집에서는 timeout 이 정상입니다.
- Gate2 는 `<GATE2_IP>`

확인:
```bash
squeue -u <YOUR_ID>
ls -t ~/Open_Duck_Playground/checkpoints_head/*.onnx | head -3
tail -20 ~/Open_Duck_Playground/logs/duck-head-910949.out
```

받아오기:
```bash
scp -i <KEY_DIR>\ubai-<YOUR_ID>.pem "<YOUR_ID>@<GATE1_IP>:/home1/<YOUR_ID>/Open_Duck_Playground/checkpoints_head/*.onnx" C:\school\2026_2\Microduck\from_ubai\
```

띄우기 (`-u` 는 키 입력 로그가 바로 보이게, `--model_path` 는 몸통 충돌 있는 씬):
```bash
cd C:\school\2026_2\Microduck\Open_Duck_Playground
set PYTHONPATH=C:\school\2026_2\Microduck\Open_Duck_Playground
..\.venv\Scripts\python.exe -u -m playground.open_duck_mini_v2.mujoco_infer -o ..\from_ubai\<파일>.onnx --model_path playground\open_duck_mini_v2\xmls\scene_standup.xml
```

---

### 2026-09-09 집 작업 — 서보 토크 여유 측정

`HARDWARE_PREP.md` §12 가 "60만원 지르기 전에 숫자로 확인" 이라고 남겨둔 것을 실제로 쟀다.
스크립트 2개 추가 (`head_probe2.py` 와 같은 방식 — 뷰어만 가짜, 제어 루프는 원본):

- `ubai/torque_probe.py` — 8구간(정지/전후진/게걸음/좌우회전/전진+회전) 토크 통계.
  ⚠️ 토크 피크는 제어주기(50Hz)가 아니라 **물리 스텝에서 난다.** `viewer.sync` 훅에서
  받으면 10배 언더샘플링이라 피크를 놓친다 → `mj_step` 을 감싸 500Hz 로 기록했다.
- `ubai/torque_derate_test.py` — `model.actuator_forcerange` 만 깎고 걷는지 보는 실험.

#### 결과: 무릎이 클램프에 붙어 산다

| 정책 | 다리 피크 | 정격 1.86 초과 | `right_knee` 클램프(3.23) |
|---|---|---|---|
| BEST_WALK_ONNX_2 | 3.23 N·m | 10.8% | **16.4%** |
| hw8 931375 | 3.23 N·m | 8.7% | **14.2%** |

정지 자세는 1.85 로 정격 안이다. 문제는 보행 중이고, 무릎이 압도적으로 최악이다.

#### 그런데 정격으로 깎아도 안 넘어진다

**통계만으로 판정하면 안 된다.** 정책이 내는 건 토크가 아니라 위치 목표다. 실물 서보는
못 내는 토크에서 넘어지는 게 아니라 추종이 나빠질 뿐이다. 그래서 직접 깎아서 걸려 봤다.

30초(정지3/전진15/좌회전6/전진6) 동안 **1.60 N·m 까지 내려도 한 번도 안 넘어졌다**
(최저 up-vector 0.98, 학습 종료 기준은 up < 0). 대신 속도가 무너진다:

| forcerange | BEST_WALK (명령 0.150) | hw8 (명령 0.222) |
|---|---|---|
| 3.23 | 0.135 m/s | 0.181 m/s |
| 1.86 | **0.078 (−42%)** | **0.057 (−69%)** |

→ 실기 리스크는 "안 걷는다" 가 아니라 "sim 보다 느리게 걷는다". **발주 진행 가능.**

#### 알게 된 것
- **빠른 명령 범위는 공짜가 아니다.** `--ref_range` 정책(hw8)이 감쇄에 더 크게 무너진다
  (−69% vs −42%). 0.222 m/s 를 뽑는 데 토크 여유를 그만큼 쓰고 있었던 것이다.
- 이 감쇄 실험은 속도와 무관한 **상수 상한**이라 실물보다 관대하다. 실제 DC 모터는
  토크-속도 곡선이 있어 속도가 붙으면 스톨 토크보다 덜 낸다. 방향은 같고 크기는 더 나쁠 것이다.
- `torques` 벌점 -0.001 이 저토크 수렴을 유도했을 거라는 §12 의 기대는 **빗나갔다.**
  `alive=20.0` 앞에서 -0.001 은 아무 힘이 없다.

#### 다음 학교 방문 때 같이 던질 것
`forcerange` 를 1.86 으로 낮춘 300M 재학습 한 판. A6000 실측 1시간 22분이라
`alive` 스윕(948600~948602) 회수하면서 같이 넣으면 된다.
`open_duck_mini_v2.xml:49` 의 `forcerange="-3.23 3.23"` 과
`joints_properties.xml` sts3215 클래스의 `-3.35 3.35` **둘 다** 고쳐야 한다.

---

### 2026-09-08 학교 방문 기록

큐가 비어 있는 것을 확인하고(`.done`, 진행 중 잡 0) `alive` 스윕 3개를 던졌다.

#### 왜 alive 를 건드리나
9/6 결론이 "가중치 조절로 더 갈 문제가 아니다" 였다. `head_pos` 를 -1 → -8 로 8배 올려도
스윙은 0.42 → 0.48 밖에 안 늘었다. yaw 1.5 오차의 비용이 18 이면 `alive=20.0` 과 대등한데도
머리를 안 돌린다 → **머리를 크게 돌렸다가 넘어져 `alive` 를 잃는 쪽이 더 손해라고 판단한다**
는 가설. 그렇다면 손봐야 할 건 `head_pos` 가 아니라 `alive` 다.

판정 기준: **스윙이 0.48 에서 올라가는가.** 안 올라가면 가설이 틀린 것이고, 원인이 다른 데
있으니 커리큘럼 설계로 넘어간다. 넘어짐이 느는 건 예상된 부작용이지 실패가 아니다 —
그것도 측정 대상이다. 회수 후 `ubai/head_probe2.py` 로 잰다.

#### 서버 변경
- `joystick.py`: `alive` 를 `ALIVE_W` 환경변수로 뺐다 (안 주면 기존 `20.0`).
  9/6 의 `HEAD_POS_W` 와 같은 방식. 백업 `joystick.py.bak.20260908`
- 제출 전 검증: `ALIVE_W=5.0 HEAD_POS_W=-8.0 .venv/bin/python -c "...default_config()..."` 로
  **환경변수가 config 까지 흘러가는지 실제 import 해서 확인**하고 던졌다.
  `os` import 누락이나 문법 오류였다면 잡 3개가 죽고 다음 방문 때나 알게 됐을 것이다.
- 스냅샷 회수: `from_ubai/server_wip_2026_09_08/` ← **9/6 의 `HEAD_POS_W` 작업이 여태
  로컬에 백업이 없었다.** 노트에 "했다" 는 기록만 있고 코드는 클러스터에만 있었다.
  9/4 스냅샷(`server_wip_2026_09_04/`)은 `head_pos=-1.0` 하드코딩 상태라 대체재가 안 된다.

#### 확인된 것
- **"최종 300M 이 279M 보다 낮다" 는 노이즈가 맞다.** hw8 로그의 마지막 8개 체크포인트 reward 가
  `198.97 → 204.18 → 198.75 → 178.31 → 165.39 → 181.85 → 196.87 → 166.66` 으로
  165~205 사이를 왕복한다 (reward_std 120~140). 정책이 퇴화한 게 아니라 그냥 분산이다.
  → **체크포인트를 reward 로 고르는 것 자체가 신뢰도가 낮다.** 뷰어 비교가 필요한 이유다.
- 서버 `joystick.py` 19번 줄 `import os` 가 **CRLF** 다 (`cat -A` 로 `import os^M$`).
  Python 은 정상 처리하니 무해하지만, `grep 'import os$'` 류의 패턴은 빗나간다.

---

### 2026-09-06 학교 방문 기록

**지금 클러스터에서 931374 / 931375 가 돌고 있습니다.** 13:50 시작, n090, gpu4, A6000.
노트북과 무관하게 계속 돕니다. 받아올 곳: `~/Open_Duck_Playground/checkpoints_hw3` / `checkpoints_hw8`

| 잡 | head_pos 가중치 | 출력 |
|---|---|---|
| 910953 (대조군, 이미 회수) | -1.0 | `checkpoints_fast` |
| 931374 | **-3.0** | `checkpoints_hw3` |
| 931375 | **-8.0** | `checkpoints_hw8` |

셋 다 나머지 설정 동일(레퍼런스 정합 범위, `flat_terrain_backlash`, 300M, WARM=none).

#### 왜 다시 던졌나 — 머리 추종 실측 (2026-09-06)
`head_yaw` 관절 가동범위는 ±2.793 rad 이라 ±1.5 명령은 여유 안이다. 그런데:

| 정책 | 명령 0 | 명령 +1.5 | 명령 -1.5 | 실제 스윙 |
|---|---|---|---|---|
| BEST_WALK_ONNX_2 | -0.230 | -0.227 | -0.230 | **0.00** |
| head 910949 | +0.224 | +0.228 | -0.205 | 0.43 |
| fast 910953 | +0.207 | +0.220 | -0.203 | 0.42 |

→ `head_pos=-1.0` 은 "완전 무시"는 깼지만 명령의 **14%**만 따라간다. 양수 명령엔 사실상 무반응.

정지 자세도 문제다. home 키프레임의 머리 4축 목표는 **전부 0** 인데 (도 단위):

| 관절 | BEST_WALK | head 910949 | fast 910953 |
|---|---|---|---|
| head_yaw | -13.2° | **+12.8°** | +11.9° |
| neck_pitch | +12.5° | +11.0° | +11.9° |
| head_pitch | -10.3° | +3.4° | +9.4° |
| head_roll | -7.8° | +4.4° | -8.2° |

`head_yaw` 는 **크기가 그대로고 부호만 뒤집혔다** — 개선이 아니다. 고개가 한쪽으로 꺾인 채 굳는 게 이것.
`alive=20.0` / `tracking_ang_vel=6.0` 대비 `head_pos=1.0` 이 너무 약하다는 결론.

#### "스스로 끄덕거린다" 는 정상이다
머리 관절 진폭: 제자리에선 0.02 수준으로 거의 정지. 걸을 때만 head_pitch 0.15 / head_yaw 0.40 로 흔들린다.
**기존 커뮤니티 정책도 동일하다**(0.148 vs 0.153). 보행 주기가 머리로 전달되는 것이고 레퍼런스 모션에 들어있다.
고칠 대상 아님.

#### 이번에 바꾼 것
- **서버** `joystick.py`: 가중치를 `HEAD_POS_W` 환경변수로 뺐다 (안 주면 기존 `-1.0`).
  백업 `joystick.py.bak.20260906`. 제출은 `--export=ALL,...,HEAD_POS_W=-3.0,...`
- **로컬** `mujoco_infer.py`: `--ref_range` 플래그 추가. **fast/rough 정책엔 반드시 붙일 것.**
  안 붙이면 뷰어가 옛 범위(x 0.15 / y 0.2)를 보내서, 전진은 0.222 를 못 쓰고 게걸음은 학습 범위 밖이라 넘어진다.
- **로컬** `scene_flat_terrain_backlash_collision.xml` 신설 (+ 모델 사본). **재생 전용.**
  걷기 모델엔 발바닥 2개 말고 충돌체가 없어서 넘어뜨리면 몸통이 바닥을 통과했다.
  standup 모델의 `*_ground_collision` 8개를 옮겨 붙였다 (body 프레임 8개 전부 동일함을 확인).
  backlash 관절 10개는 그대로 유지 — `scene_standup` 은 backlash 가 없어서 학습 조건과 다르다.
  ⚠️ **학습에는 절대 쓰지 말 것.** 충돌체가 늘면 동역학과 속도가 바뀐다. 학습은 `scene_flat_terrain_backlash.xml`.

#### 결과 (2026-09-06 15:12 완료, 같은 날 회수·실측)
둘 다 COMPLETED exit 0, 약 1시간 21분. 회수: `from_ubai/hw3_*` `from_ubai/hw8_*`

⚠️ **총 reward 로 비교하지 말 것.** `head_pos` 는 비용 항이라 가중치를 올리면 총합이 깎인다.
(-1.0: 245.4 / -3.0: 200.9 / -8.0: 166.7 — 이건 정책 품질이 아니라 비용 항 크기다.)

**정지 자세 오차 (도, 정답 0) — 명령 0 일 때**

| head_pos | neck_pitch | head_pitch | head_yaw | head_roll |
|---|---|---|---|---|
| -1.0 (910953) | +11.8 | +9.4 | +11.8 | -8.2 |
| -3.0 (931374) | +10.4 | +7.5 | -12.3 | -2.7 |
| **-8.0 (931375)** | **-2.5** | **-1.8** | **+0.7** | **-0.1** |

→ **-8.0 이 정지 자세 문제를 해결했다.** 4축 전부 2.5도 이내, head_yaw 는 0.7도.
-3.0 은 무의미했다 — head_yaw 부호만 또 뒤집히고 크기는 그대로다.
**결론: 앞으로 `HEAD_POS_W=-8.0` 을 기본으로 쓴다.**

**남은 문제: 명령 추종 이득은 여전히 낮다**

| head_pos | cmd 0 | cmd +1.5 | cmd -1.5 | 스윙 |
|---|---|---|---|---|
| -1.0 | +0.207 | +0.220 | -0.203 | 0.423 |
| -3.0 | -0.214 | +0.237 | -0.233 | 0.470 |
| -8.0 | +0.012 | +0.238 | -0.244 | 0.483 |

-8.0 은 0 을 중심으로 대칭이 됐지만, ±1.5 를 명령해도 ±0.24 밖에 안 간다 (**약 16%**).
가중치를 8배 올려도 스윙은 0.42→0.48 뿐 — **가중치 조절로 더 갈 문제가 아니다.**

확인해본 가설과 결과:
- ~~학습 때 좁은 범위만 샘플링?~~ → **아니다.** `head_range_factor=1.0` 이라 학습도 ±1.5 전 범위를 뽑는다
- `cost_head_pos` 는 4축 제곱오차 합, 상한 없음. -8.0 이면 yaw 1.5 오차의 비용이 18 로 `alive=20.0` 급이다
- 그런데도 안 따라간다 → 머리를 크게 돌리면 넘어질 위험이 커지고, `alive` 를 잃는 쪽이 더 손해라고
  판단하는 것으로 보인다. **다음 시도는 `alive` 를 낮추거나 커리큘럼을 거는 방향.**

⚠️ 미확인: -8.0 의 **보행 품질**. 전진 중 머리 진폭이 오히려 늘었다
(head_pitch 0.164→0.266, head_yaw 0.198→0.248). 뷰어로 걷는 걸 직접 봐야 한다.

실측 스크립트: `scratchpad/head_probe2.py` (뷰어 launch_passive 만 가짜로 갈아끼우고 제어 루프는 원본 사용)
#### 다음 방문 때
1. `bash ~/ubai/40_status.sh` → 931374 / 931375 결과 확인
2. `checkpoints_hw3` / `checkpoints_hw8` 의 ONNX 회수
3. 위 실측 스크립트로 다시 재서 `head_yaw` 스윙과 정지 오프셋이 개선됐는지 비교
   (`-3.0` 도 부족하면 `-8.0` 쪽을, 둘 다 부족하면 `alive` 를 낮추는 방향을 검토)

## 오늘 결과

| 잡 | 내용 | 결과 |
|---|---|---|
| 899937 | 걷기 300M | ✅ COMPLETED 1시간 11분, reward 269.8, 실측 68,700 step/s |
| 900098 | 기립 스모크 2M | ✅ exit 0, reward 43.4 |
| 900118 | 기립 v1 150M | 97M 에서 취소 (보상 결함 발견) |
| 900147 | 기립 v2 150M | ✅ COMPLETED 43분 23초, 151,388,160 step |
| 900201 | 기립 v3 150M | ✅ 완료 (v3_final / v4_final 회수됨) |

**2026-09-04**

| 잡 | 내용 | 결과 |
|---|---|---|
| 910947 | 걷기+머리 (웜스타트) | ❌ exit 1 — orbax 체크포인트 복원 불가 (아래 발견 4) |
| 910949 | 걷기+머리 300M | ✅ COMPLETED 1:22:20, reward 243.1 (279M 291.2) |
| 910953 | 걷기+머리+**속도범위 정합** 300M (평지) | ✅ COMPLETED 1:22:45, reward 245.4 (279M 289.1) |
| 910954 | 위와 같은 설정 + **험지**(rough_terrain_backlash) | ✅ COMPLETED 6:04:54, reward 231.5 (279M 244.6) |

### 이 3개를 어떻게 던졌나 (2026-09-06 에 sacct 에서 복구)
셋 다 `31_train_head.sbatch` **하나**를 환경변수로 돌려막았다. 별도 스크립트는 없다.
`WARM=none` 은 웜스타트를 끄는 값 — 910947 이 orbax 로 깨져서 처음부터 학습으로 돌렸다.
```bash
sbatch --export=ALL,WARM=none,STEPS=300000000 ~/ubai/31_train_head.sbatch
sbatch --job-name=duck-fast  --export=ALL,WARM=none,OUT=checkpoints_fast,STEPS=300000000,TASK=flat_terrain_backlash  ~/ubai/31_train_head.sbatch
sbatch --job-name=duck-rough --export=ALL,WARM=none,OUT=checkpoints_rough,STEPS=300000000,TASK=rough_terrain_backlash ~/ubai/31_train_head.sbatch
```

### ⚠️ 서버 코드가 git 에 없다 (2026-09-06 확인)
위 3개를 만들어낸 `rewards.py` / `joystick.py` 수정이 **서버 워킹트리에만 미커밋 상태**로 있었다.
서버 repo 는 upstream HEAD(`b9be205`) 기준이라 로컬의 standup 커밋 3개도 없다 — 양방향으로 갈라져 있다.
2026-09-06 에 원본 파일과 패치를 `from_ubai/server_wip_2026_09_04/` 로 회수해뒀다.
**아직 로컬 repo 에 병합하지 않았다.** 병합 시 CRLF 때문에 전체 파일 diff 로 보이니 `--ignore-cr-at-eol` 을 쓸 것.

### A6000 실측 처리량 (2026-09-04)
910949 가 16분에 42,926,080 step → **약 44,700 step/s**.
gpu1 의 RTX3090 실측 68,700 step/s 보다 **오히려 느리다.** 300M 기준 3090 71분 vs A6000 약 112분.
같은 GA102 계열인데 A6000 은 클럭이 낮아서 그렇다. VRAM 48G 도 `num_envs=8192` 엔 남아돈다.
→ **A6000 을 고를 이유는 성능이 아니라 대기시간뿐이다.** gpu1 이 한산하면 gpu1 이 낫다.
   (gpu3 의 A6000 Ada 는 AD102 라 실제로 빠르겠지만 늘 만석이다 — 이날 여유 0장/대기 56잡)

### 명령 범위가 레퍼런스 모션과 안 맞았다 (910953 에서 수정)
`polynomial_coefficients.pkl` 이 실제로 가진 범위 vs 원본 `joystick.py` 설정:

| | 레퍼런스 | 원본 설정 | 문제 |
|---|---|---|---|
| dx | [-0.148, **0.222**] | [-0.15, **0.15**] | 전진 48% 를 안 쓰고 있었다 |
| dy | [-0.111, **0.111**] | [-0.2, **0.2**] | 레퍼런스보다 80% 넓다 |
| dθ | [-1.111, 1.222] | [-1.0, 1.0] | 여유 있음, 그대로 둠 |

`vel_to_index` 가 clip 하므로 **dy 0.111~0.2 는 전부 같은 레퍼런스로 간다.** 그 구간에서
`tracking_lin_vel`(scale 2.5)은 "0.2 로 가", `imitation`(내부 w_joint_pos=15.0)은 "0.111 걸음처럼
생겨라" 라고 해서 **두 보상이 정면충돌**한다. 게걸음이 제일 어색했던 이유로 의심된다.
→ `lin_vel_x=[-0.148, 0.222]`, `lin_vel_y=[-0.111, 0.111]` 로 맞춤. **레퍼런스 재생성 불필요.**

더 빠르게 하려면 `Open_Duck_reference_motion_generator` 로 레퍼런스를 다시 뽑아야 한다.

### ⚠️ `--task rough_terrain` 은 깨져 있다
`constants.py:23` 이 가리키는 `xmls/scene_rough_terrain.xml` 이 **없다.**
`scene_rough_terrain_backlash.xml` 만 존재한다 (hfield 1cm 요철). 험지는 반드시 backlash 쪽을 쓸 것.

`from_ubai/` 에 받아둔 정책:
- `2026_08_31_144425_300482560.onnx` — **걷기 완성본 (300M)**. 화살표로 조종됨
- `v2_final_2026_08_31_164000_151388160.onnx` — **기립 v2 완주본**
- 나머지는 중간 체크포인트 (비교용)

---

## 두 가지 환경 (섞지 말 것)

| | 용도 | 버전 |
|---|---|---|
| `.venv` | **추론·뷰어 전용** | jax 0.11.1 / mujoco 3.12.0 / playground 0.2.0 |
| `.venv-train-cpu` | **학습 코드 로컬 검증용** | jax 0.6.2 / mujoco 3.3.4 / playground 0.0.5 (클러스터와 동일) |

- `.venv` 로는 `joystick.py`·`standup.py` 임포트가 **안 됩니다** (`mujoco_playground._src.collision` 이 playground 0.1.0 부터 삭제됨). 추론은 onnxruntime 만 쓰므로 최신에서 잘 돕니다.
- `.venv-train-cpu` 는 학습 코드를 클러스터에 올리기 전에 검증하는 용도입니다. **CPU 로는 100스텝에 45초라 학습은 불가능합니다.**
- 검증 스크립트: `smoke_standup.py` (환경 생성 → reset → step → 보상 검사)

---

## 조작키 (코드 주석은 AZERTY 기준이라 실제 키와 다름)

| 키 | 동작 |
|---|---|
| ↑ ↓ | 전진 / 후진 (±0.15 m/s) |
| ← → | 좌 / 우 게걸음 (±0.2 m/s) |
| **Q** / **E** | 좌회전 / 우회전 |
| **P** / **;** | 보행 주파수 ±0.1 |
| **H** | 머리 조종 모드 |
| **R** | **완전 리셋 (직접 추가)** |
| **1~5** | **춤 (헤드뱅잉 / 도리도리 / 둠칫 / 두리번 / 8자)** — 2026-09-04 추가 |
| **0** | 춤 정지 |
| **T** | 머리 직접 구동 on/off (기본 on) |
| Z 등 미매핑 키 | 속도 0 정지 |

- 한 번 누르면 유지됨. 창 포커스 필요.
- 넘어뜨리기: 몸통 더블클릭 → Ctrl+드래그

---

## 직접 수정한 파일 (원본은 전부 `.orig` 백업)

### `mujoco_infer.py`
1. **R키 = 완전 리셋** — MuJoCo 기본 Backspace 는 `mj_resetData` 로 qpos0(다리 편 자세)로 되돌리는데, 정책은 "home" 키프레임(무릎 굽힌 자세) 기준이라 즉시 넘어진다. R키는 home 복원 + 정책 내부 상태(last_action, imitation_phase, 필터) 초기화.
2. **`viewer.sync()` 를 제어 주기로 이동** — 물리 스텝마다, 즉 초당 500번 호출하고 있었다. 화면은 50Hz 면 충분.
3. **`saved_obs` 상한** — 무한히 쌓여서 장시간 실행 시 메모리를 먹었다. 노트북이 멈춘 원인으로 의심됨.

### `onnx_infer.py`
- **onnxruntime 1스레드 + spin 끄기** — 기본 설정이 코어마다 스레드를 만들고 호출 사이에 바쁜 대기를 한다. 0.03ms 짜리 추론에 16코어가 계속 돌아서 CPU 98%, 노트북 렉.

**CPU 측정 결과: 98% → 코어 1개 기준 57%, 전체 16코어 대비 3.6%**

(원인 추적 시 참고: mj_step 은 0.013~0.016ms, 추론은 0.030ms 로 예산 2ms 대비 여유가 넘쳤다. 계산량이 아니라 **대기 방식**이 문제였다.)

---

## 발견 1 — 충돌체가 발바닥 2개뿐이었다

원본 `open_duck_mini_v2.xml` 의 충돌 geom 은 `left/right_foot_bottom_tpu` 둘뿐. 시각 메시 45개는 전부 `contype=0 conaffinity=0`. 바닥은 `contype=1 conaffinity=0`.

→ 넘어지면 몸이 바닥을 통과하고, 발을 축으로 자유회전해서 **180도 뒤집힌 자세**로 끝난다.

**걷기 학습에는 문제가 아니다.** 걸을 땐 발만 닿고, `fall_termination` 때문에 넘어진 뒤 물리는 시뮬레이션되지 않는다. 충돌체를 줄인 건 MJX 속도를 위한 의도된 최적화.

## 발견 2 — 자기충돌을 다 끄면 현실에 없는 자세로 선다

처음엔 시뮬 안정성을 위해 로봇 내부 쌍을 **92쌍 전부 제외**했는데, 그러면 다리가 몸통을 그대로 통과한다. 강화학습이 그 허점을 찾아내 물리적으로 불가능한 자세로 서는 정책을 만들었다. (사용자가 눈으로 발견)

→ `make_standup_xml.py` 가 이제 **60개 자세를 조사해서 판별**한다. 90% 이상 항상 닿아 있는 쌍만 박스 근사 오차로 보고 제외, 나머지는 충돌 유지.

```
제외 2쌍   힙 ↔ 몸통 (좌우)          60/60  근사 오차
유지 9쌍   다리↔몸통 35/60, 28/60
           머리↔몸통 23/60
           발↔정강이 6/60, 3/60 ...
```
6개 시드 × 150스텝 안정성 검사 통과 (NaN 0건).

---

## 기립 태스크 설계 (`standup.py`)

`Joystick` 을 상속. **관측 구조를 그대로 물려받아** 학습 결과를 기존 뷰어로 볼 수 있다 (obs `state: (101,)` 동일).

| 항목 | 내용 |
|---|---|
| XML | `scene_standup.xml` 고정 (`constants.py` 미수정) |
| `sample_command` | 항상 0 → reset·step 양쪽 샘플링 차단 |
| `reset` | 랜덤 **수평축** + 각도 20~180° 로 기울여 z=0.22 낙하 |
| `_get_termination` | `fall_termination` 제거, NaN 만 |

### 보상 (v3 현재)
```
upright     +5.0   몸통 z축이 하늘을 향함
height      +3.0   × uprightness (머리로 서는 걸 방지)
feet_contact +1.5  × uprightness (누움→섬 사이의 빠진 중간 단계)
ang_vel_xy  -0.3   × settled (똑바로 선 뒤에만. 흔들림 억제)
alive       +0.5
action_rate -0.2
stand_still -0.3   × settled (= 차렷 자세 유도. home 자세와의 거리 + 관절 속도)
torques     -0.001
```
- `uprightness = clip(up, 0, 1)`, `settled = clip((up-0.5)*2, 0, 1)` — 딱딱한 `up > 0` 조건은 90° 근처에 보상 절벽을 만든다. 하필 일어나는 중에 가장 오래 머무는 각도.
- push 비활성, `episode_length=500` (10초)

### 시행착오 기록 (같은 실수 반복 방지)

**v1 실패 — `stand_still` 이 에피소드 내내 켜져 있었다.**
`cost_stand_still` 은 `cmd_norm < 0.01` 일 때만 적용되는데, 이 태스크는 명령이 항상 0이라 늘 켜진다. 기립에 필요한 관절 속도를 그대로 벌점했다. 측정값:
```
가만히 있음   +0.504
조금 움직임   +0.520
크게 움직임   -0.367 → 클리핑 후 0.000   ← 시도할수록 손해
```
게다가 `step()` 이 보상을 0으로 클리핑해서, 어떻게 발버둥쳐도 결과가 똑같이 0 → 학습 신호 소멸.
→ `settled` 게이트 추가로 해결.

**v1 실패 2 — 회전축을 구면에서 균일하게 뽑았다.**
축이 수직에 가까우면 아무리 크게 돌려도 안 넘어지고 방향만 바뀐다. 8번 중 2번이 선 채로 시작. → **수평면(xy)에서만** 축을 뽑도록 수정.

**v1 실패 3 — 시작 각도가 100~180° 뿐이었다.**
가장 어려운 경우만 반복하니 성공을 한 번도 경험 못 해 학습 신호가 안 생겼다.
→ **20~180° 로 확대.** 살짝 기운 건 조금만 버둥대도 복구되니 거기서 보상이 잡히고, 그 감각이 어려운 각도로 확장된다. 실전(밀림·비틀거림·벽 충돌)에도 더 맞는다.
※ 이 지적은 사용자가 먼저 했음.

**v2 결과 — "버티지만 완전히 넘어지면 못 일어남"**
- ✅ 안정성 확보: `ang_vel_xy` + `action_rate` 강화로 v1 의 불안정 해결
- ❌ 완전 전복(>120°) 복구는 미해결. 커리큘럼의 어려운 꼬리
- ❌ 현실에 없는 자세로 섬 → v3 에서 자기충돌 복원으로 대응

---

## 남은 과제

- [ ] v3 결과 확인 (자기충돌 복원 + 차렷 자세 보상이 효과 있는지)
- [ ] 완전 전복 복구가 **물리적으로 가능한지** 검증. 머리 충돌 박스가 18.4×18.6cm 넓적한 판이라 등을 대고 누우면 구르는 걸 막을 수 있다. 팔도 없어 밀어낼 수단이 다리뿐. **불가능하면 학습을 더 해도 소용없다.**
  - 참고: Microduck 은 기립을 해내지만 커스텀 액추에이터를 쓴다
- [ ] 가능하다면 시작 각도를 120~180° 에 몰아주고 에피소드를 20초로 늘려 재학습
- [ ] 걷기 정책 ↔ 기립 정책 전환 로직 (Microduck 도 별개 정책으로 출고)

---

## 외형(이루매) — 나중 작업

- 시각 메시는 질량에 기여하지 않음 (15개 body 전부 `<inertial>` 명시, base 는 geom 0개)
  → **시뮬 외형 변경은 물리에 영향 0, 재학습 불필요.** 단 실물은 다름.
- 실측 질량: trunk 698.5g / **head 406.6g (전체의 19%)** / 총 2,107g
- 큰 머리: 속 비우면 부피↑ 대비 질량은 표면적 비례. 선형 1.5배 시 +125g 추정.
  목 서보 STS3215 19kg·cm 대비 정적 부하 약 2.1kg·cm → **여유 9배, 토크는 문제 없음.**
  진짜 문제는 회전 관성 → 재학습 필요
- 짧은 다리: 서보 5개 직렬이라 불가. 대신 **TPU 스커트를 몸통에 매달아** 가림
  (다리 링크에 직접 붙이면 무릎 180° 접힘에서 간섭 + 다리 끝 질량이 관성에 직결)
- 관절 가동범위: 무릎 180° / 발목 180° / 힙피치 100° / 힙롤 50° / 힙요 60°
- 액추에이터 힘 제한: 14개 전부 ±3.23 N·m
- ⚠️ 셸 간섭은 **원본 시뮬이 못 잡는다** (충돌체가 발바닥뿐) → Onshape 간섭 검사 필요.
  단 `scene_standup.xml` 은 자기충돌이 살아 있으므로 여기서는 일부 확인 가능.


---

# 2026-09-04 — 머리(고개)는 왜 안 움직였나

## 발견 3 — 학습된 정책이 머리 명령을 **완전히 무시하고 있었다**

"고개를 돌려도 안 넘어지나"를 확인하려고 재봤더니, 애초에 **고개가 안 돌아갔다.**
정지·전진·게걸음·회전 어느 조건에서든 `head_yaw` 를 `+1.5` 로 주든 `-1.5` 로 주든
실제 관절값은 `-0.23 rad` 에서 꿈쩍도 안 한다. 오차 1.3~1.7 rad = 사실상 가동범위 전체.

| 명령 | 실제 관절 (300M 걷기 정책) |
|---|---|
| head_yaw **+1.5** | -0.23 |
| head_yaw **-1.5** | -0.23 |
| neck_pitch +1.1, head_pitch +0.78 | +0.21, -0.18 |

커뮤니티 정책 `BEST_WALK_ONNX_2.onnx` 도 **똑같다.** 즉 내 학습 문제가 아니라 원본 레포의 구조다.

### 원인 (코드에서 확인)
1. `common/rewards.py` 에 `cost_head_pos` 가 **정의만 되어 있고** `joystick.py` 의 `_get_reward` 에도
   `reward_config.scales` 에도 **등록돼 있지 않다.** → 머리를 명령대로 움직일 이유가 0
2. `custom_rewards.py` 의 `reward_imitation` 은 머리 관절을 **명시적으로 잘라낸다**
   (`jp.concatenate([joints_qpos[:5], joints_qpos[9:]])`)
3. `joystick.py:419` 의 직접 구동 `motor_targets.at[5:9].set(command[3:])` 은 **주석 처리**
4. `cost_stand_still(ignore_head=False)` 는 오히려 정지 중에 머리를 home 으로 **끌어당긴다**

머리 명령 4개는 관측(101차원 중 4개)에 들어가긴 하는데, 보상이 없으니 정책 입장에선 그냥 노이즈다.
torque/action_rate 벌점만 걸려 있으니 **가만히 두는 게 최적**이라 home 근처에 굳었다.

→ **"고개 돌려도 안 넘어져?" 의 답: 안 넘어진다. 고개를 안 돌리니까.**

## 정책을 우회해 머리를 강제로 돌리면 어디까지 버티나

`motor_targets[5:9] = command[3:]` 로 직접 구동하고, **다른 관절과 같은 서보 속도 제한**을 건 상태로 측정.
(속도 제한을 안 걸고 목표값을 순간이동시키면 실제 서보보다 가혹해져서 시뮬에서만 넘어지는 가짜 실패가 난다)

| 조건 | 결과 |
|---|---|
| `head_yaw` ±1.5 (86°) 유지 | 문제없음 (최저 up 0.99) |
| `head_roll` ±0.5 유지 | 문제없음 |
| `head_pitch` ±0.78 유지 | 문제없음 |
| `neck_pitch` +1.1 (앞으로 63°) 유지 | 문제없음 |
| **`neck_pitch` -0.34 (뒤로 19°) 유지** | **넘어짐 (완전 전복)** |
| **`neck_pitch`+`head_pitch` 최대 숙임 동시** | **넘어짐** |
| yaw 만 흔들기 0.5~3 Hz (정지·보행 모두) | **전부 문제없음** |
| 4축 전부 최대진폭 흔들기 0.8~1.2 Hz | **넘어짐** |
| 위와 같은데 pitch 진폭만 ×0.7 | 문제없음 |

**요약: 좌우(yaw/roll)는 안전하고 앞뒤(pitch)가 위험하다.** 머리가 406g / 전체의 19% 라
pitch 로 크게 젖히면 무게중심이 발 지지면 밖으로 나간다. 뒤로 젖히는 쪽이 마진이 특히 얇다.
보행 주기(1.85Hz)와의 공진은 아니다 — 위험대역이 0.8~1.2Hz 로 따로 있다.

## 뷰어에 춤 기능 추가 (`mujoco_infer.py`)

머리 직접 구동 + 안무 5종. 안무는 **정지/전진/게걸음/제자리회전 4조건에서 12초씩 돌려
넘어지지 않는 것만** 남겼다 (전부 최저 up ≥ 0.96).

| 키 | 동작 |
|---|---|
| **1** | 헤드뱅잉 (2Hz, pitch 만) |
| **2** | 도리도리 (1.5Hz, 좌우만) |
| **3** | 둠칫 (1.5Hz, 좌우 + 살짝 끄덕) |
| **4** | 두리번거리기 (0.4Hz, 느긋) |
| **5** | 머리로 8자 |
| **0** | 춤 정지 |
| **T** | 머리 직접 구동 on/off (기본 on. off 면 정책에 맡기는데 지금 정책은 머리를 무시한다) |

춤추면서 화살표로 걸어다녀도 된다. 재학습 없이 지금 정책으로 된다.

## 학습: 머리 추종 보상 추가 (잡 910949, gpu5/gpu4)

수정한 것 (원본은 `.orig` 백업):
- `common/rewards.py` — `cost_head_pos` 에 `only_when_moving` 플래그 추가.
  원본은 `move_cmd_norm > 0.01` 게이트가 박혀 있어 **제자리에선 꺼진다.** 제자리 춤엔 켜야 한다.
  (기립 v1 에서 `cost_stand_still` 게이트에 당한 것과 같은 종류의 함정)
- `joystick.py` — `head_pos=-1.0` 등록 + `_get_reward` 에 항 추가 + `stand_still` 은 `ignore_head=True`
  (안 그러면 stand_still 이 머리를 home 으로 당기고 head_pos 는 명령으로 당겨서 정면충돌)

가중치 -1.0 근거: 머리 명령이 균등분포일 때 평균 벌점 약 -1.35, 최악 -4.33. `alive=+20` 대비 안전.
`step()` 이 총보상을 0 으로 클리핑하므로 이보다 키우면 기립 v1 처럼 학습신호가 죽을 수 있다.

## ⚠️ 발견 4 — `--restore_checkpoint_path` 가 **깨져 있다**

웜스타트로 300M 걷기 체크포인트를 이어받으려다 실패 (잡 910947):
```
brax/training/checkpoint.py:154  orbax_checkpointer.restore(...)
ValueError: Expected list, got RestoreArgs(restore_type=<class 'numpy.ndarray'>, ...)
```
`common/runner.py:policy_params_fn` 이 `ocp.PyTreeCheckpointer().save()` 로 **직접** 저장하는데,
brax 의 `checkpoint.load()` 는 자기 포맷(설정 파일 동반)을 기대해서 못 읽는다.

→ **`NEXT_STEPS.md` 의 "walltime 체인 재개 가능" 은 사실이 아니다.** 300M 걷기가 한 잡에 끝나서
   체인이 한 번도 실제로 걸리지 않아 여태 안 드러났을 뿐이다. 48시간 넘는 학습을 하려면 먼저 고쳐야 한다.

## 발견 5 — `MJInferBase.get_gravity` 가 엉뚱한 센서를 읽고 있었다 (수정함)

sensor **id** 를 sensordata **주소**로 써서 `upvector` 대신 `local_linvel` 을 읽었다.
그래서 서 있든 뒤집혔든 늘 0 근처가 나온다. gyro/accelerometer 는 `sensor_adr` 로 제대로 변환하는데
여기만 빠졌다. → `self.model.sensor_adr[self.gravity_id]` 로 수정.

**학습에는 영향 없다.** 학습 쪽(`base.py`)은 `mjx_env.get_sensor_data` 를 써서 처음부터 정상이고,
추론 쪽에서도 obs 에 gravity 가 주석 처리돼 있어 아무도 안 쓰고 있었다.
