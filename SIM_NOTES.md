# 시뮬레이션 / 기립(standup) 작업 노트

> 이 파일은 시뮬·외형·하드웨어 담당 세션이 관리합니다.
> `ubai/`, `NEXT_STEPS.md`, `from_ubai/` 는 UBAI 담당 세션이 만든 것이니 지우지 마세요.
> (한 번 `ubai/` 를 지웠다가 복구한 적 있음)

---

## 스스로 목표지점까지 가기 (2026-09-23)

방향키 없이 **출발점에서 도착점까지 혼자 간다.** 순서는 셋이다.

1. `scan` — 제자리에서 돌며 사람(발목 밴드)을 찾는다. 도착점 좌표를 알면
   가까운 쪽으로 돈다. 세 프레임 연속 잡혀야 진짜로 본 것으로 친다.
2. `go` — 찾은 뒤로는 **기존 추종과 똑같은 코드**(`follow_step`)다. 방향은
   매 제어스텝 카메라로 새로 잰다.
3. `arrived` — 0.55 m 안에 들면 명령을 0 으로 두고 선다.

```bash
cd Open_Duck_Playground && PYTHONPATH=. ../.venv/Scripts/python.exe   playground/open_duck_mini_v2/mujoco_infer.py   -o ../from_ubai/head_2026_09_04_140643_300482560.onnx   --model_path playground/open_duck_mini_v2/xmls/scene_person.xml   --person 1.90 0.15 --start_yaw random --goto
```

뷰어에서 `N` 이 이 기능이다. `--start_yaw random` 이면 매번 아무 방향이나 보고
시작한다 — 켰을 때 오리가 사람 쪽을 보고 있을 이유가 없으니 그게 실제 조건이다.

### 결과 (`eval_goto.py`, 장애물 없음, head 910949)

| 조건 | 결과 |
|---|---|
| 출발 방위 0/90/180/-90° | **4/4 도착** (선회 0.1~5.3초, 총 14.0~19.0초) |
| 출발 방위 무작위 8판 | **8/8 도착** |
| 도착점 (-1.20, 1.50) 3판 | **3/3 도착** |
| 가는 도중 몸통을 ±90/150/180° 돌려놓기 | **4/4 재포착 1.7~5.0초, 전부 도착** |

넘어진 판 없음 (최저 up 0.991~0.996).

### 여기서 나온 것 세 개

**1. 요 명령에 사각지대가 있다. 0.5 아래는 제자리다.**
scene_person.xml, 12초 제자리 회전 (명령 → 회전속도):

| 명령 | 0.35 | 0.40 | 0.50 | 0.55 | 0.70 | 0.80 | 1.00 |
|---|---|---|---|---|---|---|---|
| 08-31 | 4°/s | 5 | 8 | 11 | 19 | 24 | 33 |
| head 910949 | 0°/s | 1 | 4 | 9 | 23 | 29 | 39 |

`FOLLOW_SEARCH` 가 **0.35 였다. 그 값은 아무 일도 안 한다.** 표적을 놓쳤을 때
훑는 동작이 여태 실행은 되는데 오리는 가만히 서 있었던 것이다. 0.8 로 올렸다
(한 바퀴 13초). 걸어가면서 주는 작은 요 명령(`FOLLOW_KP`)은 이 사각지대와
별개다 — 전진 중에는 작은 값도 먹는다. **제자리 선회만 문턱이 높다.**

**2. 카메라 거리는 도착 판정에 못 쓴다.**
`band_tracker` 의 거리는 양쪽 밴드를 합친 덩어리의 **가로폭**으로 낸다. 비스듬히
보면 두 발목이 겹쳐 폭이 줄고 거리가 부풀려진다. 참값 1.92 m 일 때 2.18 m,
화면 가장자리에 걸리면 8.48 m 까지 튀었다. 가까이서 더 나쁘다 — 카메라는
0.375 m 에 10° 숙여 있고 밴드는 0.09 m 라, 0.4 m 안쪽이면 밴드가 화각 아래로
빠진다. 그래서 추정거리가 0.55 에 닿기 전에 표적을 잃고 **사람을 지나쳐 계속
걸어갔다** (목표 (-1.2, 1.5) 에서 0.36 m 까지 파고들고도 도착 판정이 안 났다).

지금은 **카메라 거리와 도착점 좌표 중 먼저 닿는 쪽**으로 선다. 좌표만 봐도 안
된다 — 카메라가 먼저 "다 왔다"며 다리를 세워버리는데(`follow_step` 의
`FOLLOW_STOP_M`) 좌표로는 0.60 m 라 판정이 안 나고 그 자리에 굳었다.

**3. fr186 으로는 이 기능이 아예 안 돈다.**
회전 권한이 없어서 (제자리 5°/s) 40초를 훑어도 반 바퀴를 못 돈다. 네 판 중 세
판이 "사람 못 찾음" 이었다. `eval_goto.py` 와 이 기능의 기본 정책은
**head 910949** 다 (`--ref_range` 붙이지 말 것). 아래 "fr186 은 요 제어가
무너졌다" 와 같은 이야기다.

### 방향을 무엇으로 정하는가 (헷갈리기 쉬운 곳)

**IMU 로 정하지 않는다.** 진행 방향은 매 제어스텝 카메라에서 새로 뽑는다
(밴드 방위 + 머리 요). 그래서 누가 오리를 건드려 방위가 틀어져도 다음 프레임에
바로 보정된다 — 위 표의 ±180° 비틀기 시험이 그것이다. 자세(IMU/오도메트리)가
쓰이는 곳은 딱 둘이고 **둘 다 보조**다:

- 표적이 안 보이는 동안 어느 쪽이었는지 기억 (`target_world_deg`)
- 도착 판정 (좌표까지 0.55 m)

드리프트가 쌓여도 사람이 보이는 순간 방향은 카메라가 다시 잡는다. 도착 판정만
좌표에 기대는데, 2 m 거리에서 오도메트리 오차가 정지 반경(0.55 m)을 먹을 만큼
쌓이지는 않는다.

### 새 파일

| 파일 | 역할 |
|---|---|
| `scene_person.xml` | scene_obstacles.xml 에서 **장애물만 뺀** 씬. 그 외는 동일 |
| `eval_goto.py` | 출발점 → 도착점 자율 이동 채점 (`--random N`, `--twist T DEG`) |

장애물은 아직 안 얹었다. 빈 바닥에서 도착도 못 하면 회피를 켜 봐야 실패 원인을
가를 수 없다. 다음이 `scene_obstacles.xml` 에서 같은 시험을 돌리는 것인데,
거기서는 "벽이 정면" 배치가 아직 아홉 조합 전부 실패다 (아래 표).

---

## 다시 시작할 때 (2026-09-23 갱신)

**시각 추종을 붙였다.** 걷기 정책은 손대지 않았다 — 지각은 정책 **밖**의 층이라
재학습이 없다. 정책은 자기가 받는 3개 숫자가 방향키에서 왔는지 카메라에서 왔는지
구분하지 못한다.

뷰어에서 바로 볼 수 있다 (`PYTHONPATH=.` 없으면 `No module named 'playground'` 난다):

```bash
cd Open_Duck_Playground && PYTHONPATH=. ../.venv/Scripts/python.exe   playground/open_duck_mini_v2/mujoco_infer.py   -o ../from_ubai/fr186_2026_09_12_174638_300482560.onnx --ref_range   --model_path playground/open_duck_mini_v2/xmls/scene_obstacles.xml
```

`F` 추종 · `V` 회피 · `G` 머리조준 · 방향키 누르면 수동으로 넘어옴.

### 새 파일

| 파일 | 역할 |
|---|---|
| `band_tracker.py` | 발목 형광밴드 → 방위각. **mujoco 안 씀** |
| `floor_scan.py` | 바닥 채도로 방위별 여유거리. **mujoco 안 씀** |
| `eval_bearing.py` | 색추종을 정답과 대조 |
| `eval_follow.py` | 루프 닫고 실제로 가까워지는지 채점 |
| `capture_head_cam.py` | 카메라 흔들림을 도 단위로 측정 |
| `make_video.py` | 3인칭 + 머리시점 GIF |

앞의 두 개가 Pi 로 그대로 옮겨갈 파일이다. mujoco 를 한 줄이라도 import 하면
이식할 때 다시 짜야 해서 일부러 밖에 뒀다.

### 확인된 숫자

- 색추종 방위: 정지 절대평균 0.85°, **보행 중 0.25° / 검출률 98%**
- 바닥스캔 거리: 레이캐스트 정답 대비 **절대평균 0.12 m**
- 카메라 흔들림: 피치 진폭 18.5°, 롤 14.1° (세로화각 49° 의 38%)
- 바닥/장애물 분리는 **밝기가 아니라 채도**로. 이 씬 측정값:
  바닥 0.003(그림자·격자선 포함) · 하늘 0.112 · 장애물 0.21 · 사람 0.27 · 밴드 0.87
  ⚠ 실제 바닥은 채도가 0 이 아니다. 현장에서 같은 방식으로 다시 잡을 것.
- **카메라는 0.54 m 안쪽 바닥을 못 본다** (화면 아래 끝). 그 안의 장애물은
  실제보다 멀다고 보고된다 — 위험한 방향의 오차라 회피는 그 전에 결정해야 한다.

### 되는 것 / 안 되는 것

| 배치 | 결과 |
|---|---|
| 턱이 옆에 (2.4, 0.9) | 회피 OFF 1.60 m 에서 막힘 → **회피 ON 0.52 m 도달** |
| 벽이 정면 (1.9, 0.15) | **아홉 조합 전부 넘어짐** |

벽 배치는 **지각 문제가 아니다.** 벽은 내내 잘 보인다(여유 0.62/0.51/0.48).
몸을 돌려 피하려다 사람이 화각(±31°) 밖으로 빠지고 → 방향 잃고 제자리 선회 →
넘어진다. 표적을 최대 80% 놓친다.

### 내일 할 것 (이 순서대로)

**0. 방금 넣었는데 아직 안 재본 것 두 개부터 채점한다.** `mujoco_infer.py` 에
   들어가 있고 코드는 도는데 숫자가 없다.

   - `self.face_target = True` — **몸통을 표적에 고정하고 우회는 게걸음으로만.**
     지금까지 게걸음을 켰을 때도 `ang` 을 `go_b`(갈 방향)에 물려서 몸이 같이
     돌았고 그게 사람을 화각 밖으로 밀어낸 원인이었다. 몸을 안 돌리면 사람이
     시야에서 빠질 일이 없다. 대신 우회 속도가 게걸음 한계(0.111)에 묶인다.
   - `self.target_world_deg` — **월드 기준 방위 기억.** 요를 적분해 들고 있으면
     안 보이는 동안에도 사람이 어느 쪽인지 안다. 막 훑는 것보다 낫다.
     실기에서는 자이로 z 적분이라 드리프트가 쌓이지만 몇 초 버티는 용도라 충분.

   ```bash
   .venv\Scripts\python.exe eval_follow.py --person 1.9 0.15 --quiet --seconds 30
   ```
   판정선: **벽 배치에서 안 넘어지면** 방향이 맞은 것이다.

**1. 뷰어 세그폴트.** 최종 기본값으로 띄운 창이 exit 139 로 죽었다. 로그가
   368 바이트뿐이라 시작 출력도 안 남았다. 추종용 `mujoco.Renderer` 를 뷰어가
   떠 있는 상태에서 만드는 부분이 의심스럽다 — 따로 최소 예제로 테스트했을 땐
   통과했으므로 재현부터 할 것.

### 하지 말 것 (이미 재봤고 나빠졌다)

- **머리 데드밴드** — 벽 배치 놓침 35% → 53%, 턱 배치 1.59 → 1.86 m. 뺐다.
- **여유거리 시간 최솟값 필터** — 사방이 막힌 걸로 보여 제자리에서 돌다 넘어졌다.
  원인은 흔들림이 아니었다(정지 측정은 안정적: 1.20 m→0.98, 0.95 m→0.73).
- **고정 각폭 통로 판정(12°)** — 1 m 에서 42 cm 통로를 요구한다. 오리는 20 cm 도
  안 된다. 거리에 따라 각폭을 계산해야 한다.
- **광각 렌즈만 넓히기** — `fovy` 49 → 75 → 100 을 320x240 그대로 두고 재봤다.
  벽 배치 놓침이 32% → 55% → 40% 로 **오히려 나빠졌고** 턱 배치는 그대로였다.
  화각을 넓히면 같은 해상도가 넓은 각에 퍼져 밴드가 픽셀로 작아진다. 100° 면
  초점거리가 101 px 이라 1.9 m 의 밴드 덩어리가 15 px 밖에 안 되고,
  `band_tracker.MIN_PIXELS = 25` 에 걸려 미검출이 는다.
  ⚠ 다시 볼 거면 **해상도를 같이 올려서** 재야 한다 (640x480 이상). 화각만
  넓히는 건 각해상도를 파는 짓이다.

### 기본값과 근거

`머리조준 0.5(사람과 갈 방향의 가운데) · 게걸음 OFF · 회피 ON · 요게인 0.020`

전체 표는 `mujoco_infer.py` 의 `HEAD_AIM_BLEND` 주석에 있다. 감으로 되돌리지 말 것.

### 함정 하나 (또 밟지 말 것)

채점 스크립트가 `run()` 의 제어 루프를 복제하고 있었고, 그 복사본이 DIRECT_HEAD
블록을 빠뜨려서 **머리 명령이 모터에 안 실렸다.** 머리 추종이 통째로 무효였는데
겉으론 "효과가 없네" 로만 보였다. 지금은 `MjInfer.control_step()` 하나를 뷰어와
채점이 같이 쓴다. **제어 루프를 다시 복제하지 말 것.**

---

## (이전) 다시 시작할 때 — 기립 (2026-09-15 갱신)

**방향 전환 — 하드웨어 제작은 접고 시뮬 쪽으로 간다.** 지금 작업은 **누운 상태에서 일어서기**.

오늘 기립 v2/v3/v4 를 처음으로 각도별로 채점해 봤고 (`eval_standup.py` 신규),
**45도를 넘으면 셋 다 성공률 0/8** 이었다. 150M step 짜리 학습 세 번을 쓰고 20도짜리
비틀거림만 복구한다. 원인은 학습량이 아니라 태스크 설계 두 군데였고, 둘 다 고쳤다
(아래 "2026-09-15" 항목).

⚠️ **다음에 학교 가면 할 것** — `ubai_standup/README.md` 에 scp 목록부터 채점까지 정리해뒀다.
`standup_poses.npy` 를 같이 보내야 잡이 돈다.

```bash
# 스모크 2M -> 확인되면 본학습 300M (gpu1 기준 약 73분)
sbatch --export=ALL,SEGMENT=300000000,TOTAL=300000000,OUTDIR=checkpoints_standup_v5 \
  $HOME/ubai_standup/30_train_standup.sbatch
```

판정선: **90도 이상에서 성공률이 0 을 넘으면** 설계 수정이 먹힌 것이다.

아래 걷기 쪽 상태(09-12)는 그대로 둔다 — 회수·측정이 아직 안 끝났다.

---

## (이전) 다시 시작할 때 — 걷기 (2026-09-12 갱신)

**클러스터에서 잡 5개가 돌고 있습니다** (계정 한도는 동시 10잡 / 12 GPU — 확인함).
전부 토크 상한을 관절별로 랜덤화한 300M 학습이고, 이유와 판정 기준은 아래 "두 번째 잡" 항목.

| 잡 | 바꾼 것 | 출력 디렉토리 | 예상 |
|---|---|---|---|
| 973253 `duck-frr` | U(1.40, 1.90) ← **기준** | `checkpoints_frr` | 18:51 시작, ~1.5h |
| 973273 `duck-frr-n` | U(1.55, 1.90) 좁게 | `checkpoints_frr_n` | ~1.5h |
| 973274 `duck-frr-w` | U(1.25, 2.00) 넓게 | `checkpoints_frr_w` | ~1.5h |
| 973275 `duck-frr-s2` | U(1.40, 1.90) + **SEED=1** | `checkpoints_frr_s2` | ~1.5h |
| 973276 `duck-rough-fr` | U(1.40, 1.90) + **험지** | `checkpoints_rough_fr` | **~6h** |

- 973273/274 는 **랜덤화 폭**을 한 번에 가른다. 폭이 좁으면 fr186 처럼 한 점에 과적합하고,
  넓으면 평균을 맞추느라 어디서도 안 빠르다. 셋을 같은 하네스로 재면 무릎이 보인다.
- 973275 는 **시드만 다른 복제**다. 밴드 간 차이가 시드 노이즈인지 아닌지 이것 없이는 못 가른다
  (279M/300M 보상 차이를 노이즈로 판정했던 것과 같은 함정).
- 973276 은 축이 다르다 — 실제 바닥은 평평하지 않다. 6시간짜리라 오늘 안 끝난다.

앞선 972703(`duck-fr186`)은 17:46:52 에 COMPLETED (1시간 17분 06초, exit 0),
ONNX 는 `from_ubai/fr186_*.onnx` (279M / 300M) 로 회수했고 **측정까지 끝냈습니다.**

**실기용 정책은 이제 `fr186_2026_09_12_174638_300482560.onnx` 입니다.**
정격 1.86 N·m 에서 **0.130 m/s** — hw8(0.057)의 **2.3배**입니다. 근거는 아래 "정격으로 학습하니
속도를 되찾았다" 항목.

⚠️ **다음에 할 것 (이 순서대로)**
0. 973253 회수 (`checkpoints_frr/*.onnx`) → `torque_derate_test.py` 로 1.86 / 1.50 두 지점 비교.
   **학교 네트워크에서만 받을 수 있습니다.**
1. fr186 의 **머리 추종은 아직 안 쟀습니다.** `ubai/head_probe2.py` 로 hw8 과 비교하세요.
   토크 상한을 깎았으니 머리를 덜 돌릴 가능성이 있고, 그러면 9/6~9/8 의 head_pos·alive 작업과
   맞물립니다.
2. `alive` 스윕(alive2 / alive5 / alive10, 9/10 회수)은 여전히 미측정입니다. 다만 우선순위는
   내려갑니다 — 스윕은 머리 스윙 문제고, 보행·실기 쪽은 fr186 으로 정리됐습니다.
3. 보행 품질 뷰어 확인은 이제 **fr186 300M 을 정격 1.86 으로 깎은 상태**로 보세요.
   그게 실기에서 보게 될 걸음입니다. 279M 과는 차이가 없었습니다 (0.133 vs 0.130).
4. 학교 갈 일은 지금 없습니다. 던질 거리가 생기면 그때.

> (이전 기록: 09-08 alive 스윕 / 09-06 head_pos 스윕 / 09-04 걷기+머리추종 / 08-31 기립 v3)

```bash
ssh <YOUR_ID>@<GATE1_IP> -i <KEY_DIR>\ubai-<YOUR_ID>.pem
```
- ⚠️ **학교 네트워크에서만 접속됩니다** (사설 IP, VPN 없음). 집에서는 timeout 이 정상입니다.
- Gate2 는 `<GATE2_IP>`

확인:
```bash
squeue -u <YOUR_ID>
ls -t ~/Open_Duck_Playground/checkpoints_head/*.onnx | head -3
tail -20 ~/Open_Duck_Playground/logs/duck-head-910949.out
```

받아오기:
```bash
scp -i <KEY_DIR>\ubai-<YOUR_ID>.pem "<YOUR_ID>@<GATE1_IP>:/home1/<YOUR_ID>/Open_Duck_Playground/checkpoints_head/*.onnx" C:\school\2026_2\Microduck\from_ubai\
```

띄우기 (`-u` 는 키 입력 로그가 바로 보이게, `--model_path` 는 몸통 충돌 있는 씬):
```bash
cd C:\school\2026_2\Microduck\Open_Duck_Playground
set PYTHONPATH=C:\school\2026_2\Microduck\Open_Duck_Playground
..\.venv\Scripts\python.exe -u -m playground.open_duck_mini_v2.mujoco_infer -o ..\from_ubai\<파일>.onnx --model_path playground\open_duck_mini_v2\xmls\scene_standup.xml
```

---

### 2026-09-09 집 작업 — 서보 토크 여유 측정

`HARDWARE_PREP.md` §12 가 "60만원 지르기 전에 숫자로 확인" 이라고 남겨둔 것을 실제로 쟀다.
스크립트 2개 추가 (`head_probe2.py` 와 같은 방식 — 뷰어만 가짜, 제어 루프는 원본):

- `ubai/torque_probe.py` — 8구간(정지/전후진/게걸음/좌우회전/전진+회전) 토크 통계.
  ⚠️ 토크 피크는 제어주기(50Hz)가 아니라 **물리 스텝에서 난다.** `viewer.sync` 훅에서
  받으면 10배 언더샘플링이라 피크를 놓친다 → `mj_step` 을 감싸 500Hz 로 기록했다.
- `ubai/torque_derate_test.py` — `model.actuator_forcerange` 만 깎고 걷는지 보는 실험.

#### 결과: 무릎이 클램프에 붙어 산다

| 정책 | 다리 피크 | 정격 1.86 초과 | `right_knee` 클램프(3.23) |
|---|---|---|---|
| BEST_WALK_ONNX_2 | 3.23 N·m | 10.8% | **16.4%** |
| hw8 931375 | 3.23 N·m | 8.7% | **14.2%** |

정지 자세는 1.85 로 정격 안이다. 문제는 보행 중이고, 무릎이 압도적으로 최악이다.

#### 그런데 정격으로 깎아도 안 넘어진다

**통계만으로 판정하면 안 된다.** 정책이 내는 건 토크가 아니라 위치 목표다. 실물 서보는
못 내는 토크에서 넘어지는 게 아니라 추종이 나빠질 뿐이다. 그래서 직접 깎아서 걸려 봤다.

30초(정지3/전진15/좌회전6/전진6) 동안 **1.60 N·m 까지 내려도 한 번도 안 넘어졌다**
(최저 up-vector 0.98, 학습 종료 기준은 up < 0). 대신 속도가 무너진다:

| forcerange | BEST_WALK (명령 0.150) | hw8 (명령 0.222) |
|---|---|---|
| 3.23 | 0.135 m/s | 0.181 m/s |
| 1.86 | **0.078 (−42%)** | **0.057 (−69%)** |

→ 실기 리스크는 "안 걷는다" 가 아니라 "sim 보다 느리게 걷는다". **발주 진행 가능.**

#### 알게 된 것
- **빠른 명령 범위는 공짜가 아니다.** `--ref_range` 정책(hw8)이 감쇄에 더 크게 무너진다
  (−69% vs −42%). 0.222 m/s 를 뽑는 데 토크 여유를 그만큼 쓰고 있었던 것이다.
- 이 감쇄 실험은 속도와 무관한 **상수 상한**이라 실물보다 관대하다. 실제 DC 모터는
  토크-속도 곡선이 있어 속도가 붙으면 스톨 토크보다 덜 낸다. 방향은 같고 크기는 더 나쁠 것이다.
- `torques` 벌점 -0.001 이 저토크 수렴을 유도했을 거라는 §12 의 기대는 **빗나갔다.**
  `alive=20.0` 앞에서 -0.001 은 아무 힘이 없다.

#### 다음 학교 방문 때 같이 던질 것 → 2026-09-12 에 던졌다 (972703)
`forcerange` 를 1.86 으로 낮춘 300M 재학습 한 판. 3090 이 비어 있어서 A6000(1시간 22분) 대신
gpu1 에 들어갔다 — 약 75분.

⚠️ **여기 적어뒀던 파일 두 개는 틀렸다.** `open_duck_mini_v2.xml:49` 와 `joints_properties.xml` 은
`--task flat_terrain` 쪽이고, 우리가 쓰는 `flat_terrain_backlash` 는 둘 다 안 읽는다.
`scene_flat_terrain_backlash.xml` → `open_duck_mini_v2_backlash.xml` 인데, 이 파일은
`joints_properties.xml` 을 include 하지 않고 `sts3215` 클래스를 자기 안에 복사해 갖고 있다.
실제로 고쳐야 하는 곳은 **`open_duck_mini_v2_backlash.xml:50` 한 줄뿐**이다.
그대로 믿고 고쳤으면 hw8 과 똑같은 잡을 75분 돌리고 다음 방문 때나 알아챘을 것이다.

---

### 2026-09-12 학교 방문 기록

큐가 비어 있는 것(`squeue` 0줄)을 확인하고 토크 정격 재학습 한 판을 던졌다.

#### 서버 변경
- `xmls/open_duck_mini_v2_backlash.xml:50` 의 `forcerange="-3.23 3.23"` → **`"-1.86 1.86"`**.
  `kp="17.11"` 은 건드리지 않았다 — 바뀌는 건 토크 상한 하나뿐이어야 한다.
  백업 `open_duck_mini_v2_backlash.xml.bak.20260912`
- 제출 전 검증 (9/8 과 같이 **실제로 import 해서** 확인하고 던졌다):
  ```bash
  HEAD_POS_W=-8.0 JAX_PLATFORMS=cpu .venv/bin/python -c "...task_to_xml + default_config..."
  # nu: 14   forcerange rows: [[-1.86  1.86]]   head_pos: -8.0   alive: 20.0
  ```
  액추에이터 14개가 전부 1.86 이고 보상 가중치가 hw8 과 같다는 것까지 보고 던졌다.
- 제출:
  ```bash
  sbatch --job-name=duck-fr186 --partition=gpu1,gpu4,gpu5     --export=ALL,WARM=none,OUT=checkpoints_fr186,STEPS=300000000,TASK=flat_terrain_backlash,HEAD_POS_W=-8.0     ~/ubai/31_train_head.sbatch
  ```
  파티션을 셋 다 걸어 먼저 비는 쪽으로 보냈다. gpu1 이 잡혔다(n009).
- 실측 처리량 **72,000 step/s** (16:42 21.4M → 16:47 42.9M). 08-31 걷기 잡의 3090 실측 68,700 과 같은 급이다.
  ⚠️ **첫 구간(step 0 → 21.4M)만 보면 50,400 으로 나온다.** 거기엔 학습 루프 JIT 컴파일이 섞여 있다.
  처리량은 반드시 **두 번째 이후 구간**으로 재고, 300M 은 컴파일 포함 약 75분으로 잡으면 된다.
- 스냅샷 회수: `from_ubai/server_wip_2026_09_12/` (joystick.py / rewards.py / 바꾼 XML / `git diff`).
  서버 코드가 git 에 없는 상태는 그대로다 — 9/6, 9/8 에 이은 세 번째 스냅샷이다.

#### 결과: 정격으로 학습하니 속도를 되찾았다 (같은 날 18:46 회수·측정)

COMPLETED 1:17:06, exit 0. `torque_derate_test.py` 와 같은 하네스로 30초(정지3/전진15/좌회전6/전진6)
돌린 결과 — **명령은 셋 다 0.222 m/s, 아무도 안 넘어졌다**:

| 정책 | forcerange 3.23 | **forcerange 1.86 (실물)** |
|---|---|---|
| hw8 (3.23 으로 학습) | 0.181 m/s | **0.057 (−69%)** |
| fr186 300M (1.86 으로 학습) | 0.136 m/s | **0.130 (−4%)** |
| fr186 279M | 0.148 m/s | 0.133 (−10%) |

- **정격에서 2.3배(0.130 vs 0.057).** 9/9 에 "실기 리스크는 느리게 걷는 것" 이라고 했던 그 손실을
  학습으로 거의 다 되찾았다. 실기 정책은 fr186 으로 간다.
- fr186 은 상한을 풀어줘도(3.23) 0.136 밖에 안 나온다. 없는 토크를 쓰는 걸음을 아예 안 배웠다.
  hw8 이 3.23 에서 빨랐던 건 실물에 없는 토크를 쓰고 있었다는 뜻이다.
- **보상으로 고르면 틀린다.** 체크포인트 평균 보상은 fr186 168 < hw8 179 (−6%) 인데
  실물 조건 속도는 2.3배다. 제약이 다른 두 모델의 보상은 애초에 비교 대상이 아니다.
- 279M vs 300M 은 fr186 에서도 차이가 없다 (0.133 vs 0.130). 9/8 의 "노이즈" 결론과 일치한다.

#### 두 번째 잡: 토크 상한 랜덤화 (973253, 18:51 제출)

fr186 을 정격 아래로 더 깎아 보니 **딱 1.86 에만 맞춰져 있었다** (같은 하네스, 명령 0.222):

| forcerange | fr186 전진 | 대응하는 실물 상황 |
|---|---|---|
| 1.86 | 0.130 m/s | 7.4V 정격 |
| 1.71 | 0.111 (−15%) | 배터리 6.8V 로 처짐 |
| 1.60 | 0.103 (−21%) | |
| 1.50 | 0.086 (−34%) | 서보 발열 감쇄 |
| 1.40 | 0.061 (−53%) | 발열 + 전압 처짐 |

한 번도 안 넘어지지만(up 0.99 유지) 속도가 계속 깎인다. sim2real 튜닝은 수십~수백 회 반복이라
그동안 서보는 뜨거워지고 배터리는 처진다 — 실기가 사는 곳은 1.86 한 점이 아니라 **1.4~1.9 구간**이다.

`randomize.py` 는 질량 ±10%, kp ±10%, 마찰, CoM, 액션 지연 0~3스텝까지 이미 랜덤화하면서
**토크 상한만 고정**이었다. 방금 제일 크게 작용한다고 확인된 그 값이다.

- 서버 `playground/common/randomize.py` 에 `FR_RAND_LO` / `FR_RAND_HI` 추가.
  **안 주면 XML 값 그대로**라 기존 잡들과 동작이 같다 (9/6 `HEAD_POS_W`, 9/8 `ALIVE_W` 와 같은 방식).
  관절별로 따로 뽑으므로 서보 **개체차**까지 같이 덮는다. 백업 `randomize.py.bak.20260912`
- 제출 전 검증 — 켠 경우와 끈 경우를 둘 다 돌려서 확인하고 던졌다:
  ```
  FR_RAND_LO=1.40 FR_RAND_HI=1.90 → (8, 14, 2), hi 1.400~1.894, env 마다 관절마다 다름
  (env 변수 없음)                 → hi 전부 1.860, 기존 잡과 동일
  ```
  `mjx` 가 이 값을 실제로 쓰는 것도 확인했다 (`mjx/_src/forward.py:219` 에서 clip).
- 제출:
  ```bash
  sbatch --job-name=duck-frr --partition=gpu1,gpu4,gpu5     --export=ALL,WARM=none,OUT=checkpoints_frr,STEPS=300000000,TASK=flat_terrain_backlash,  HEAD_POS_W=-8.0,FR_RAND_LO=1.40,FR_RAND_HI=1.90 ~/ubai/31_train_head.sbatch
  ```
- **판정 기준**: 1.86 에서 0.130 을 지키면서 **1.50 에서 0.086 보다 확실히 높은가.**
  전 구간에서 fr186 보다 나쁘면 랜덤화 폭이 과한 것이다. 그때는 fr186 을 쓰고
  속도 저하는 전원·발열 설계 문제로 넘긴다 (벤치 급전, 서보 휴식 간격).

#### 같이 던진 것 — 밴드 스윕 + 시드 복제 + 험지 (18:58, 973273~973276)

한 번의 방문에서 답이 나오게 4개를 더 던졌다. 계정 한도는 **동시 10잡 / 제출 20잡 / 12 GPU**
(`sacctmgr show assoc` 로 확인). 지금 5잡이니 여유가 있다.

- 서버 `playground/common/runner.py` 에 `SEED` 환경변수 추가 (안 주면 기존 값 = 재현 가능).
  `ppo.train` 에 시드가 안 넘어가고 있어서 **복제 실험 자체가 불가능했다.**
  백업 `runner.py.bak.20260912`
- 던지는 순서상 주의: `31_train_head.sbatch` 는 `OUT` 이 비어 있을 때만 처음부터 학습한다.
  같은 `OUT` 으로 두 번 던지면 앞 잡 체크포인트를 이어받아 **비교가 깨진다.** 전부 다른 `OUT` 을 줬다.

측정은 전부 로컬에서 `torque_derate_test.py` 하나로 한다 — 1.86 과 1.50 두 지점만 보면 된다.

#### 다음 방문 때
1. (완료) `squeue -u $USER` 로 972703 종료 확인 → `checkpoints_fr186/*.onnx` 회수
2. (완료) 감쇄 조건 속도 비교 → 위 표. 되찾았다
3. 남은 것: fr186 의 머리 추종 측정 (`head_probe2.py`). 서버에 던질 거리는 지금 없다

---

### 2026-09-08 학교 방문 기록

큐가 비어 있는 것을 확인하고(`.done`, 진행 중 잡 0) `alive` 스윕 3개를 던졌다.

#### 왜 alive 를 건드리나
9/6 결론이 "가중치 조절로 더 갈 문제가 아니다" 였다. `head_pos` 를 -1 → -8 로 8배 올려도
스윙은 0.42 → 0.48 밖에 안 늘었다. yaw 1.5 오차의 비용이 18 이면 `alive=20.0` 과 대등한데도
머리를 안 돌린다 → **머리를 크게 돌렸다가 넘어져 `alive` 를 잃는 쪽이 더 손해라고 판단한다**
는 가설. 그렇다면 손봐야 할 건 `head_pos` 가 아니라 `alive` 다.

판정 기준: **스윙이 0.48 에서 올라가는가.** 안 올라가면 가설이 틀린 것이고, 원인이 다른 데
있으니 커리큘럼 설계로 넘어간다. 넘어짐이 느는 건 예상된 부작용이지 실패가 아니다 —
그것도 측정 대상이다. 회수 후 `ubai/head_probe2.py` 로 잰다.

#### 서버 변경
- `joystick.py`: `alive` 를 `ALIVE_W` 환경변수로 뺐다 (안 주면 기존 `20.0`).
  9/6 의 `HEAD_POS_W` 와 같은 방식. 백업 `joystick.py.bak.20260908`
- 제출 전 검증: `ALIVE_W=5.0 HEAD_POS_W=-8.0 .venv/bin/python -c "...default_config()..."` 로
  **환경변수가 config 까지 흘러가는지 실제 import 해서 확인**하고 던졌다.
  `os` import 누락이나 문법 오류였다면 잡 3개가 죽고 다음 방문 때나 알게 됐을 것이다.
- 스냅샷 회수: `from_ubai/server_wip_2026_09_08/` ← **9/6 의 `HEAD_POS_W` 작업이 여태
  로컬에 백업이 없었다.** 노트에 "했다" 는 기록만 있고 코드는 클러스터에만 있었다.
  9/4 스냅샷(`server_wip_2026_09_04/`)은 `head_pos=-1.0` 하드코딩 상태라 대체재가 안 된다.

#### 확인된 것
- **"최종 300M 이 279M 보다 낮다" 는 노이즈가 맞다.** hw8 로그의 마지막 8개 체크포인트 reward 가
  `198.97 → 204.18 → 198.75 → 178.31 → 165.39 → 181.85 → 196.87 → 166.66` 으로
  165~205 사이를 왕복한다 (reward_std 120~140). 정책이 퇴화한 게 아니라 그냥 분산이다.
  → **체크포인트를 reward 로 고르는 것 자체가 신뢰도가 낮다.** 뷰어 비교가 필요한 이유다.
- 서버 `joystick.py` 19번 줄 `import os` 가 **CRLF** 다 (`cat -A` 로 `import os^M$`).
  Python 은 정상 처리하니 무해하지만, `grep 'import os$'` 류의 패턴은 빗나간다.

---

### 2026-09-06 학교 방문 기록

**지금 클러스터에서 931374 / 931375 가 돌고 있습니다.** 13:50 시작, n090, gpu4, A6000.
노트북과 무관하게 계속 돕니다. 받아올 곳: `~/Open_Duck_Playground/checkpoints_hw3` / `checkpoints_hw8`

| 잡 | head_pos 가중치 | 출력 |
|---|---|---|
| 910953 (대조군, 이미 회수) | -1.0 | `checkpoints_fast` |
| 931374 | **-3.0** | `checkpoints_hw3` |
| 931375 | **-8.0** | `checkpoints_hw8` |

셋 다 나머지 설정 동일(레퍼런스 정합 범위, `flat_terrain_backlash`, 300M, WARM=none).

#### 왜 다시 던졌나 — 머리 추종 실측 (2026-09-06)
`head_yaw` 관절 가동범위는 ±2.793 rad 이라 ±1.5 명령은 여유 안이다. 그런데:

| 정책 | 명령 0 | 명령 +1.5 | 명령 -1.5 | 실제 스윙 |
|---|---|---|---|---|
| BEST_WALK_ONNX_2 | -0.230 | -0.227 | -0.230 | **0.00** |
| head 910949 | +0.224 | +0.228 | -0.205 | 0.43 |
| fast 910953 | +0.207 | +0.220 | -0.203 | 0.42 |

→ `head_pos=-1.0` 은 "완전 무시"는 깼지만 명령의 **14%**만 따라간다. 양수 명령엔 사실상 무반응.

정지 자세도 문제다. home 키프레임의 머리 4축 목표는 **전부 0** 인데 (도 단위):

| 관절 | BEST_WALK | head 910949 | fast 910953 |
|---|---|---|---|
| head_yaw | -13.2° | **+12.8°** | +11.9° |
| neck_pitch | +12.5° | +11.0° | +11.9° |
| head_pitch | -10.3° | +3.4° | +9.4° |
| head_roll | -7.8° | +4.4° | -8.2° |

`head_yaw` 는 **크기가 그대로고 부호만 뒤집혔다** — 개선이 아니다. 고개가 한쪽으로 꺾인 채 굳는 게 이것.
`alive=20.0` / `tracking_ang_vel=6.0` 대비 `head_pos=1.0` 이 너무 약하다는 결론.

#### "스스로 끄덕거린다" 는 정상이다
머리 관절 진폭: 제자리에선 0.02 수준으로 거의 정지. 걸을 때만 head_pitch 0.15 / head_yaw 0.40 로 흔들린다.
**기존 커뮤니티 정책도 동일하다**(0.148 vs 0.153). 보행 주기가 머리로 전달되는 것이고 레퍼런스 모션에 들어있다.
고칠 대상 아님.

#### 이번에 바꾼 것
- **서버** `joystick.py`: 가중치를 `HEAD_POS_W` 환경변수로 뺐다 (안 주면 기존 `-1.0`).
  백업 `joystick.py.bak.20260906`. 제출은 `--export=ALL,...,HEAD_POS_W=-3.0,...`
- **로컬** `mujoco_infer.py`: `--ref_range` 플래그 추가. **fast/rough 정책엔 반드시 붙일 것.**
  안 붙이면 뷰어가 옛 범위(x 0.15 / y 0.2)를 보내서, 전진은 0.222 를 못 쓰고 게걸음은 학습 범위 밖이라 넘어진다.
- **로컬** `scene_flat_terrain_backlash_collision.xml` 신설 (+ 모델 사본). **재생 전용.**
  걷기 모델엔 발바닥 2개 말고 충돌체가 없어서 넘어뜨리면 몸통이 바닥을 통과했다.
  standup 모델의 `*_ground_collision` 8개를 옮겨 붙였다 (body 프레임 8개 전부 동일함을 확인).
  backlash 관절 10개는 그대로 유지 — `scene_standup` 은 backlash 가 없어서 학습 조건과 다르다.
  ⚠️ **학습에는 절대 쓰지 말 것.** 충돌체가 늘면 동역학과 속도가 바뀐다. 학습은 `scene_flat_terrain_backlash.xml`.

#### 결과 (2026-09-06 15:12 완료, 같은 날 회수·실측)
둘 다 COMPLETED exit 0, 약 1시간 21분. 회수: `from_ubai/hw3_*` `from_ubai/hw8_*`

⚠️ **총 reward 로 비교하지 말 것.** `head_pos` 는 비용 항이라 가중치를 올리면 총합이 깎인다.
(-1.0: 245.4 / -3.0: 200.9 / -8.0: 166.7 — 이건 정책 품질이 아니라 비용 항 크기다.)

**정지 자세 오차 (도, 정답 0) — 명령 0 일 때**

| head_pos | neck_pitch | head_pitch | head_yaw | head_roll |
|---|---|---|---|---|
| -1.0 (910953) | +11.8 | +9.4 | +11.8 | -8.2 |
| -3.0 (931374) | +10.4 | +7.5 | -12.3 | -2.7 |
| **-8.0 (931375)** | **-2.5** | **-1.8** | **+0.7** | **-0.1** |

→ **-8.0 이 정지 자세 문제를 해결했다.** 4축 전부 2.5도 이내, head_yaw 는 0.7도.
-3.0 은 무의미했다 — head_yaw 부호만 또 뒤집히고 크기는 그대로다.
**결론: 앞으로 `HEAD_POS_W=-8.0` 을 기본으로 쓴다.**

**남은 문제: 명령 추종 이득은 여전히 낮다**

| head_pos | cmd 0 | cmd +1.5 | cmd -1.5 | 스윙 |
|---|---|---|---|---|
| -1.0 | +0.207 | +0.220 | -0.203 | 0.423 |
| -3.0 | -0.214 | +0.237 | -0.233 | 0.470 |
| -8.0 | +0.012 | +0.238 | -0.244 | 0.483 |

-8.0 은 0 을 중심으로 대칭이 됐지만, ±1.5 를 명령해도 ±0.24 밖에 안 간다 (**약 16%**).
가중치를 8배 올려도 스윙은 0.42→0.48 뿐 — **가중치 조절로 더 갈 문제가 아니다.**

확인해본 가설과 결과:
- ~~학습 때 좁은 범위만 샘플링?~~ → **아니다.** `head_range_factor=1.0` 이라 학습도 ±1.5 전 범위를 뽑는다
- `cost_head_pos` 는 4축 제곱오차 합, 상한 없음. -8.0 이면 yaw 1.5 오차의 비용이 18 로 `alive=20.0` 급이다
- 그런데도 안 따라간다 → 머리를 크게 돌리면 넘어질 위험이 커지고, `alive` 를 잃는 쪽이 더 손해라고
  판단하는 것으로 보인다. **다음 시도는 `alive` 를 낮추거나 커리큘럼을 거는 방향.**

⚠️ 미확인: -8.0 의 **보행 품질**. 전진 중 머리 진폭이 오히려 늘었다
(head_pitch 0.164→0.266, head_yaw 0.198→0.248). 뷰어로 걷는 걸 직접 봐야 한다.

실측 스크립트: `scratchpad/head_probe2.py` (뷰어 launch_passive 만 가짜로 갈아끼우고 제어 루프는 원본 사용)
#### 다음 방문 때
1. `bash ~/ubai/40_status.sh` → 931374 / 931375 결과 확인
2. `checkpoints_hw3` / `checkpoints_hw8` 의 ONNX 회수
3. 위 실측 스크립트로 다시 재서 `head_yaw` 스윙과 정지 오프셋이 개선됐는지 비교
   (`-3.0` 도 부족하면 `-8.0` 쪽을, 둘 다 부족하면 `alive` 를 낮추는 방향을 검토)

## 오늘 결과

| 잡 | 내용 | 결과 |
|---|---|---|
| 899937 | 걷기 300M | ✅ COMPLETED 1시간 11분, reward 269.8, 실측 68,700 step/s |
| 900098 | 기립 스모크 2M | ✅ exit 0, reward 43.4 |
| 900118 | 기립 v1 150M | 97M 에서 취소 (보상 결함 발견) |
| 900147 | 기립 v2 150M | ✅ COMPLETED 43분 23초, 151,388,160 step |
| 900201 | 기립 v3 150M | ✅ 완료 (v3_final / v4_final 회수됨) |

**2026-09-04**

| 잡 | 내용 | 결과 |
|---|---|---|
| 910947 | 걷기+머리 (웜스타트) | ❌ exit 1 — orbax 체크포인트 복원 불가 (아래 발견 4) |
| 910949 | 걷기+머리 300M | ✅ COMPLETED 1:22:20, reward 243.1 (279M 291.2) |
| 910953 | 걷기+머리+**속도범위 정합** 300M (평지) | ✅ COMPLETED 1:22:45, reward 245.4 (279M 289.1) |
| 910954 | 위와 같은 설정 + **험지**(rough_terrain_backlash) | ✅ COMPLETED 6:04:54, reward 231.5 (279M 244.6) |

### 이 3개를 어떻게 던졌나 (2026-09-06 에 sacct 에서 복구)
셋 다 `31_train_head.sbatch` **하나**를 환경변수로 돌려막았다. 별도 스크립트는 없다.
`WARM=none` 은 웜스타트를 끄는 값 — 910947 이 orbax 로 깨져서 처음부터 학습으로 돌렸다.
```bash
sbatch --export=ALL,WARM=none,STEPS=300000000 ~/ubai/31_train_head.sbatch
sbatch --job-name=duck-fast  --export=ALL,WARM=none,OUT=checkpoints_fast,STEPS=300000000,TASK=flat_terrain_backlash  ~/ubai/31_train_head.sbatch
sbatch --job-name=duck-rough --export=ALL,WARM=none,OUT=checkpoints_rough,STEPS=300000000,TASK=rough_terrain_backlash ~/ubai/31_train_head.sbatch
```

### ⚠️ 서버 코드가 git 에 없다 (2026-09-06 확인)
위 3개를 만들어낸 `rewards.py` / `joystick.py` 수정이 **서버 워킹트리에만 미커밋 상태**로 있었다.
서버 repo 는 upstream HEAD(`b9be205`) 기준이라 로컬의 standup 커밋 3개도 없다 — 양방향으로 갈라져 있다.
2026-09-06 에 원본 파일과 패치를 `from_ubai/server_wip_2026_09_04/` 로 회수해뒀다.
**아직 로컬 repo 에 병합하지 않았다.** 병합 시 CRLF 때문에 전체 파일 diff 로 보이니 `--ignore-cr-at-eol` 을 쓸 것.

### A6000 실측 처리량 (2026-09-04)
910949 가 16분에 42,926,080 step → **약 44,700 step/s**.
gpu1 의 RTX3090 실측 68,700 step/s 보다 **오히려 느리다.** 300M 기준 3090 71분 vs A6000 약 112분.
같은 GA102 계열인데 A6000 은 클럭이 낮아서 그렇다. VRAM 48G 도 `num_envs=8192` 엔 남아돈다.
→ **A6000 을 고를 이유는 성능이 아니라 대기시간뿐이다.** gpu1 이 한산하면 gpu1 이 낫다.
   (gpu3 의 A6000 Ada 는 AD102 라 실제로 빠르겠지만 늘 만석이다 — 이날 여유 0장/대기 56잡)

### 명령 범위가 레퍼런스 모션과 안 맞았다 (910953 에서 수정)
`polynomial_coefficients.pkl` 이 실제로 가진 범위 vs 원본 `joystick.py` 설정:

| | 레퍼런스 | 원본 설정 | 문제 |
|---|---|---|---|
| dx | [-0.148, **0.222**] | [-0.15, **0.15**] | 전진 48% 를 안 쓰고 있었다 |
| dy | [-0.111, **0.111**] | [-0.2, **0.2**] | 레퍼런스보다 80% 넓다 |
| dθ | [-1.111, 1.222] | [-1.0, 1.0] | 여유 있음, 그대로 둠 |

`vel_to_index` 가 clip 하므로 **dy 0.111~0.2 는 전부 같은 레퍼런스로 간다.** 그 구간에서
`tracking_lin_vel`(scale 2.5)은 "0.2 로 가", `imitation`(내부 w_joint_pos=15.0)은 "0.111 걸음처럼
생겨라" 라고 해서 **두 보상이 정면충돌**한다. 게걸음이 제일 어색했던 이유로 의심된다.
→ `lin_vel_x=[-0.148, 0.222]`, `lin_vel_y=[-0.111, 0.111]` 로 맞춤. **레퍼런스 재생성 불필요.**

더 빠르게 하려면 `Open_Duck_reference_motion_generator` 로 레퍼런스를 다시 뽑아야 한다.

### ⚠️ `--task rough_terrain` 은 깨져 있다
`constants.py:23` 이 가리키는 `xmls/scene_rough_terrain.xml` 이 **없다.**
`scene_rough_terrain_backlash.xml` 만 존재한다 (hfield 1cm 요철). 험지는 반드시 backlash 쪽을 쓸 것.

`from_ubai/` 에 받아둔 정책:
- `2026_08_31_144425_300482560.onnx` — **걷기 완성본 (300M)**. 화살표로 조종됨
- `v2_final_2026_08_31_164000_151388160.onnx` — **기립 v2 완주본**
- 나머지는 중간 체크포인트 (비교용)

---

## 두 가지 환경 (섞지 말 것)

| | 용도 | 버전 |
|---|---|---|
| `.venv` | **추론·뷰어 전용** | jax 0.11.1 / mujoco 3.12.0 / playground 0.2.0 |
| `.venv-train-cpu` | **학습 코드 로컬 검증용** | jax 0.6.2 / mujoco 3.3.4 / playground 0.0.5 (클러스터와 동일) |

- `.venv` 로는 `joystick.py`·`standup.py` 임포트가 **안 됩니다** (`mujoco_playground._src.collision` 이 playground 0.1.0 부터 삭제됨). 추론은 onnxruntime 만 쓰므로 최신에서 잘 돕니다.
- `.venv-train-cpu` 는 학습 코드를 클러스터에 올리기 전에 검증하는 용도입니다. **CPU 로는 100스텝에 45초라 학습은 불가능합니다.**
- 검증 스크립트: `smoke_standup.py` (환경 생성 → reset → step → 보상 검사)

---

## 조작키 (코드 주석은 AZERTY 기준이라 실제 키와 다름)

| 키 | 동작 |
|---|---|
| ↑ ↓ | 전진 / 후진 (±0.15 m/s) |
| ← → | 좌 / 우 게걸음 (±0.2 m/s) |
| **Q** / **E** | 좌회전 / 우회전 |
| **P** / **;** | 보행 주파수 ±0.1 |
| **H** | 머리 조종 모드 |
| **R** | **완전 리셋 (직접 추가)** |
| **1~5** | **춤 (헤드뱅잉 / 도리도리 / 둠칫 / 두리번 / 8자)** — 2026-09-04 추가 |
| **0** | 춤 정지 |
| **T** | 머리 직접 구동 on/off (기본 on) |
| Z 등 미매핑 키 | 속도 0 정지 |

- 한 번 누르면 유지됨. 창 포커스 필요.
- 넘어뜨리기: 몸통 더블클릭 → Ctrl+드래그

---

## 직접 수정한 파일 (원본은 전부 `.orig` 백업)

### `mujoco_infer.py`
1. **R키 = 완전 리셋** — MuJoCo 기본 Backspace 는 `mj_resetData` 로 qpos0(다리 편 자세)로 되돌리는데, 정책은 "home" 키프레임(무릎 굽힌 자세) 기준이라 즉시 넘어진다. R키는 home 복원 + 정책 내부 상태(last_action, imitation_phase, 필터) 초기화.
2. **`viewer.sync()` 를 제어 주기로 이동** — 물리 스텝마다, 즉 초당 500번 호출하고 있었다. 화면은 50Hz 면 충분.
3. **`saved_obs` 상한** — 무한히 쌓여서 장시간 실행 시 메모리를 먹었다. 노트북이 멈춘 원인으로 의심됨.

### `onnx_infer.py`
- **onnxruntime 1스레드 + spin 끄기** — 기본 설정이 코어마다 스레드를 만들고 호출 사이에 바쁜 대기를 한다. 0.03ms 짜리 추론에 16코어가 계속 돌아서 CPU 98%, 노트북 렉.

**CPU 측정 결과: 98% → 코어 1개 기준 57%, 전체 16코어 대비 3.6%**

(원인 추적 시 참고: mj_step 은 0.013~0.016ms, 추론은 0.030ms 로 예산 2ms 대비 여유가 넘쳤다. 계산량이 아니라 **대기 방식**이 문제였다.)

---

## 발견 1 — 충돌체가 발바닥 2개뿐이었다

원본 `open_duck_mini_v2.xml` 의 충돌 geom 은 `left/right_foot_bottom_tpu` 둘뿐. 시각 메시 45개는 전부 `contype=0 conaffinity=0`. 바닥은 `contype=1 conaffinity=0`.

→ 넘어지면 몸이 바닥을 통과하고, 발을 축으로 자유회전해서 **180도 뒤집힌 자세**로 끝난다.

**걷기 학습에는 문제가 아니다.** 걸을 땐 발만 닿고, `fall_termination` 때문에 넘어진 뒤 물리는 시뮬레이션되지 않는다. 충돌체를 줄인 건 MJX 속도를 위한 의도된 최적화.

## 발견 2 — 자기충돌을 다 끄면 현실에 없는 자세로 선다

처음엔 시뮬 안정성을 위해 로봇 내부 쌍을 **92쌍 전부 제외**했는데, 그러면 다리가 몸통을 그대로 통과한다. 강화학습이 그 허점을 찾아내 물리적으로 불가능한 자세로 서는 정책을 만들었다. (사용자가 눈으로 발견)

→ `make_standup_xml.py` 가 이제 **60개 자세를 조사해서 판별**한다. 90% 이상 항상 닿아 있는 쌍만 박스 근사 오차로 보고 제외, 나머지는 충돌 유지.

```
제외 2쌍   힙 ↔ 몸통 (좌우)          60/60  근사 오차
유지 9쌍   다리↔몸통 35/60, 28/60
           머리↔몸통 23/60
           발↔정강이 6/60, 3/60 ...
```
6개 시드 × 150스텝 안정성 검사 통과 (NaN 0건).

---

## 기립 태스크 설계 (`standup.py`)

`Joystick` 을 상속. **관측 구조를 그대로 물려받아** 학습 결과를 기존 뷰어로 볼 수 있다 (obs `state: (101,)` 동일).

| 항목 | 내용 |
|---|---|
| XML | `scene_standup.xml` 고정 (`constants.py` 미수정) |
| `sample_command` | 항상 0 → reset·step 양쪽 샘플링 차단 |
| `reset` | 랜덤 **수평축** + 각도 20~180° 로 기울여 z=0.22 낙하 |
| `_get_termination` | `fall_termination` 제거, NaN 만 |

### 보상 (v3 현재)
```
upright     +5.0   몸통 z축이 하늘을 향함
height      +3.0   × uprightness (머리로 서는 걸 방지)
feet_contact +1.5  × uprightness (누움→섬 사이의 빠진 중간 단계)
ang_vel_xy  -0.3   × settled (똑바로 선 뒤에만. 흔들림 억제)
alive       +0.5
action_rate -0.2
stand_still -0.3   × settled (= 차렷 자세 유도. home 자세와의 거리 + 관절 속도)
torques     -0.001
```
- `uprightness = clip(up, 0, 1)`, `settled = clip((up-0.5)*2, 0, 1)` — 딱딱한 `up > 0` 조건은 90° 근처에 보상 절벽을 만든다. 하필 일어나는 중에 가장 오래 머무는 각도.
- push 비활성, `episode_length=500` (10초)

### 시행착오 기록 (같은 실수 반복 방지)

**v1 실패 — `stand_still` 이 에피소드 내내 켜져 있었다.**
`cost_stand_still` 은 `cmd_norm < 0.01` 일 때만 적용되는데, 이 태스크는 명령이 항상 0이라 늘 켜진다. 기립에 필요한 관절 속도를 그대로 벌점했다. 측정값:
```
가만히 있음   +0.504
조금 움직임   +0.520
크게 움직임   -0.367 → 클리핑 후 0.000   ← 시도할수록 손해
```
게다가 `step()` 이 보상을 0으로 클리핑해서, 어떻게 발버둥쳐도 결과가 똑같이 0 → 학습 신호 소멸.
→ `settled` 게이트 추가로 해결.

**v1 실패 2 — 회전축을 구면에서 균일하게 뽑았다.**
축이 수직에 가까우면 아무리 크게 돌려도 안 넘어지고 방향만 바뀐다. 8번 중 2번이 선 채로 시작. → **수평면(xy)에서만** 축을 뽑도록 수정.

**v1 실패 3 — 시작 각도가 100~180° 뿐이었다.**
가장 어려운 경우만 반복하니 성공을 한 번도 경험 못 해 학습 신호가 안 생겼다.
→ **20~180° 로 확대.** 살짝 기운 건 조금만 버둥대도 복구되니 거기서 보상이 잡히고, 그 감각이 어려운 각도로 확장된다. 실전(밀림·비틀거림·벽 충돌)에도 더 맞는다.
※ 이 지적은 사용자가 먼저 했음.

**v2 결과 — "버티지만 완전히 넘어지면 못 일어남"**
- ✅ 안정성 확보: `ang_vel_xy` + `action_rate` 강화로 v1 의 불안정 해결
- ❌ 완전 전복(>120°) 복구는 미해결. 커리큘럼의 어려운 꼬리
- ❌ 현실에 없는 자세로 섬 → v3 에서 자기충돌 복원으로 대응

---

## 남은 과제

- [ ] v3 결과 확인 (자기충돌 복원 + 차렷 자세 보상이 효과 있는지)
- [ ] 완전 전복 복구가 **물리적으로 가능한지** 검증. 머리 충돌 박스가 18.4×18.6cm 넓적한 판이라 등을 대고 누우면 구르는 걸 막을 수 있다. 팔도 없어 밀어낼 수단이 다리뿐. **불가능하면 학습을 더 해도 소용없다.**
  - 참고: Microduck 은 기립을 해내지만 커스텀 액추에이터를 쓴다
- [ ] 가능하다면 시작 각도를 120~180° 에 몰아주고 에피소드를 20초로 늘려 재학습
- [ ] 걷기 정책 ↔ 기립 정책 전환 로직 (Microduck 도 별개 정책으로 출고)

---

## 외형(이루매) — 나중 작업

- 시각 메시는 질량에 기여하지 않음 (15개 body 전부 `<inertial>` 명시, base 는 geom 0개)
  → **시뮬 외형 변경은 물리에 영향 0, 재학습 불필요.** 단 실물은 다름.
- 실측 질량: trunk 698.5g / **head 406.6g (전체의 19%)** / 총 2,107g
- 큰 머리: 속 비우면 부피↑ 대비 질량은 표면적 비례. 선형 1.5배 시 +125g 추정.
  목 서보 STS3215 19kg·cm 대비 정적 부하 약 2.1kg·cm → **여유 9배, 토크는 문제 없음.**
  진짜 문제는 회전 관성 → 재학습 필요
- 짧은 다리: 서보 5개 직렬이라 불가. 대신 **TPU 스커트를 몸통에 매달아** 가림
  (다리 링크에 직접 붙이면 무릎 180° 접힘에서 간섭 + 다리 끝 질량이 관성에 직결)
- 관절 가동범위: 무릎 180° / 발목 180° / 힙피치 100° / 힙롤 50° / 힙요 60°
- 액추에이터 힘 제한: 14개 전부 ±3.23 N·m
- ⚠️ 셸 간섭은 **원본 시뮬이 못 잡는다** (충돌체가 발바닥뿐) → Onshape 간섭 검사 필요.
  단 `scene_standup.xml` 은 자기충돌이 살아 있으므로 여기서는 일부 확인 가능.


---

# 2026-09-04 — 머리(고개)는 왜 안 움직였나

## 발견 3 — 학습된 정책이 머리 명령을 **완전히 무시하고 있었다**

"고개를 돌려도 안 넘어지나"를 확인하려고 재봤더니, 애초에 **고개가 안 돌아갔다.**
정지·전진·게걸음·회전 어느 조건에서든 `head_yaw` 를 `+1.5` 로 주든 `-1.5` 로 주든
실제 관절값은 `-0.23 rad` 에서 꿈쩍도 안 한다. 오차 1.3~1.7 rad = 사실상 가동범위 전체.

| 명령 | 실제 관절 (300M 걷기 정책) |
|---|---|
| head_yaw **+1.5** | -0.23 |
| head_yaw **-1.5** | -0.23 |
| neck_pitch +1.1, head_pitch +0.78 | +0.21, -0.18 |

커뮤니티 정책 `BEST_WALK_ONNX_2.onnx` 도 **똑같다.** 즉 내 학습 문제가 아니라 원본 레포의 구조다.

### 원인 (코드에서 확인)
1. `common/rewards.py` 에 `cost_head_pos` 가 **정의만 되어 있고** `joystick.py` 의 `_get_reward` 에도
   `reward_config.scales` 에도 **등록돼 있지 않다.** → 머리를 명령대로 움직일 이유가 0
2. `custom_rewards.py` 의 `reward_imitation` 은 머리 관절을 **명시적으로 잘라낸다**
   (`jp.concatenate([joints_qpos[:5], joints_qpos[9:]])`)
3. `joystick.py:419` 의 직접 구동 `motor_targets.at[5:9].set(command[3:])` 은 **주석 처리**
4. `cost_stand_still(ignore_head=False)` 는 오히려 정지 중에 머리를 home 으로 **끌어당긴다**

머리 명령 4개는 관측(101차원 중 4개)에 들어가긴 하는데, 보상이 없으니 정책 입장에선 그냥 노이즈다.
torque/action_rate 벌점만 걸려 있으니 **가만히 두는 게 최적**이라 home 근처에 굳었다.

→ **"고개 돌려도 안 넘어져?" 의 답: 안 넘어진다. 고개를 안 돌리니까.**

## 정책을 우회해 머리를 강제로 돌리면 어디까지 버티나

`motor_targets[5:9] = command[3:]` 로 직접 구동하고, **다른 관절과 같은 서보 속도 제한**을 건 상태로 측정.
(속도 제한을 안 걸고 목표값을 순간이동시키면 실제 서보보다 가혹해져서 시뮬에서만 넘어지는 가짜 실패가 난다)

| 조건 | 결과 |
|---|---|
| `head_yaw` ±1.5 (86°) 유지 | 문제없음 (최저 up 0.99) |
| `head_roll` ±0.5 유지 | 문제없음 |
| `head_pitch` ±0.78 유지 | 문제없음 |
| `neck_pitch` +1.1 (앞으로 63°) 유지 | 문제없음 |
| **`neck_pitch` -0.34 (뒤로 19°) 유지** | **넘어짐 (완전 전복)** |
| **`neck_pitch`+`head_pitch` 최대 숙임 동시** | **넘어짐** |
| yaw 만 흔들기 0.5~3 Hz (정지·보행 모두) | **전부 문제없음** |
| 4축 전부 최대진폭 흔들기 0.8~1.2 Hz | **넘어짐** |
| 위와 같은데 pitch 진폭만 ×0.7 | 문제없음 |

**요약: 좌우(yaw/roll)는 안전하고 앞뒤(pitch)가 위험하다.** 머리가 406g / 전체의 19% 라
pitch 로 크게 젖히면 무게중심이 발 지지면 밖으로 나간다. 뒤로 젖히는 쪽이 마진이 특히 얇다.
보행 주기(1.85Hz)와의 공진은 아니다 — 위험대역이 0.8~1.2Hz 로 따로 있다.

## 뷰어에 춤 기능 추가 (`mujoco_infer.py`)

머리 직접 구동 + 안무 5종. 안무는 **정지/전진/게걸음/제자리회전 4조건에서 12초씩 돌려
넘어지지 않는 것만** 남겼다 (전부 최저 up ≥ 0.96).

| 키 | 동작 |
|---|---|
| **1** | 헤드뱅잉 (2Hz, pitch 만) |
| **2** | 도리도리 (1.5Hz, 좌우만) |
| **3** | 둠칫 (1.5Hz, 좌우 + 살짝 끄덕) |
| **4** | 두리번거리기 (0.4Hz, 느긋) |
| **5** | 머리로 8자 |
| **0** | 춤 정지 |
| **T** | 머리 직접 구동 on/off (기본 on. off 면 정책에 맡기는데 지금 정책은 머리를 무시한다) |

춤추면서 화살표로 걸어다녀도 된다. 재학습 없이 지금 정책으로 된다.

## 학습: 머리 추종 보상 추가 (잡 910949, gpu5/gpu4)

수정한 것 (원본은 `.orig` 백업):
- `common/rewards.py` — `cost_head_pos` 에 `only_when_moving` 플래그 추가.
  원본은 `move_cmd_norm > 0.01` 게이트가 박혀 있어 **제자리에선 꺼진다.** 제자리 춤엔 켜야 한다.
  (기립 v1 에서 `cost_stand_still` 게이트에 당한 것과 같은 종류의 함정)
- `joystick.py` — `head_pos=-1.0` 등록 + `_get_reward` 에 항 추가 + `stand_still` 은 `ignore_head=True`
  (안 그러면 stand_still 이 머리를 home 으로 당기고 head_pos 는 명령으로 당겨서 정면충돌)

가중치 -1.0 근거: 머리 명령이 균등분포일 때 평균 벌점 약 -1.35, 최악 -4.33. `alive=+20` 대비 안전.
`step()` 이 총보상을 0 으로 클리핑하므로 이보다 키우면 기립 v1 처럼 학습신호가 죽을 수 있다.

## ⚠️ 발견 4 — `--restore_checkpoint_path` 가 **깨져 있다**

웜스타트로 300M 걷기 체크포인트를 이어받으려다 실패 (잡 910947):
```
brax/training/checkpoint.py:154  orbax_checkpointer.restore(...)
ValueError: Expected list, got RestoreArgs(restore_type=<class 'numpy.ndarray'>, ...)
```
`common/runner.py:policy_params_fn` 이 `ocp.PyTreeCheckpointer().save()` 로 **직접** 저장하는데,
brax 의 `checkpoint.load()` 는 자기 포맷(설정 파일 동반)을 기대해서 못 읽는다.

→ **`NEXT_STEPS.md` 의 "walltime 체인 재개 가능" 은 사실이 아니다.** 300M 걷기가 한 잡에 끝나서
   체인이 한 번도 실제로 걸리지 않아 여태 안 드러났을 뿐이다. 48시간 넘는 학습을 하려면 먼저 고쳐야 한다.

## 발견 5 — `MJInferBase.get_gravity` 가 엉뚱한 센서를 읽고 있었다 (수정함)

sensor **id** 를 sensordata **주소**로 써서 `upvector` 대신 `local_linvel` 을 읽었다.
그래서 서 있든 뒤집혔든 늘 0 근처가 나온다. gyro/accelerometer 는 `sensor_adr` 로 제대로 변환하는데
여기만 빠졌다. → `self.model.sensor_adr[self.gravity_id]` 로 수정.

**학습에는 영향 없다.** 학습 쪽(`base.py`)은 `mjx_env.get_sensor_data` 를 써서 처음부터 정상이고,
추론 쪽에서도 obs 에 gravity 가 주석 처리돼 있어 아무도 안 쓰고 있었다.

---

# 2026-09-15 — 기립: 45도에서 막힌 진짜 이유

> 하드웨어 제작은 접고 시뮬 쪽으로 방향을 잡았다. 첫 목표는 **누운 상태에서 시작해
> 일어서기**. 그런데 v2/v3/v4 를 그냥 더 돌리기 전에, 먼저 지금 뭘 못 하는지부터 쟀다.

## 먼저 잰다 — `eval_standup.py`

여태 기립 정책은 뷰어로만 봤다. 뷰어로는 "되는 것 같다 / 아닌 것 같다" 까지밖에 안 나오고,
**어느 각도에서 벽이 생기는지** 모르면 다음 학습을 어떻게 고쳐야 할지도 모른다.

그래서 헤드리스 채점기를 만들었다. 제어 루프는 `mujoco_infer.py` 와 동일하게 두고
(50Hz, action_scale 0.25, 서보 속도 제한 5.24 rad/s) 뷰어만 뺐다. 시작 자세는
`standup.py` 의 reset 과 같다. 성공 = **마지막 1.5초 내내** up > 0.9 이고 높이 > 12cm.

```
.venv\Scripts\python.exe eval_standup.py -o from_ubai\v4_final_151388160.onnx
```

### 결과: 세 정책 모두 45도 이상에서 0%

각도당 8회, 10초. (`SIM_NOTES` 의 "v2 는 버티지만 완전히 넘어지면 못 일어남" 은
**낙관적인 기록이었다.** 버티는 것도 20도까지다.)

| 시작각 | v2 | v3 | v4 |
|---|---|---|---|
| 20° | 4/8 | 2/8 | 3/8 |
| 45° | 0/8 | 0/8 | 0/8 |
| 70°~180° | 0/8 | 0/8 | 0/8 |

150M step 짜리 학습 세 번을 쓰고 20도짜리 비틀거림만 복구한다. **학습량 문제가 아니다.**

### 학습 보상은 이걸 전혀 안 알려주고 있었다

09-15 에 클러스터 로그를 전부 회수해서(`from_ubai/slurm_logs/`) 학습 곡선과 대조해 봤다.

| 정책 | 잡 | 최종 학습 보상 | 45도 이상 성공률 |
|---|---|---|---|
| v2 | 900147 | **85.1** | 0/8 |
| v3 | 900201 | 83.0 | 0/8 |
| v4 | 901992 | 76.4 | 0/8 |

**보상이 제일 높은 v2 가 제일 낫지도 않다.** 구버전 보상표를 에피소드 전체에 대입하면
(실제 `episode_length` 는 1000, dt 0.02):

```
가만히 누워만 있으면   60.0
처음부터 서 있으면    260.0
v2 85.1 -> 누움~서있음 구간의 12.5% 지점 / v3 11.5% / v4 8.2%
```

**받은 보상의 70% 이상이 그냥 누워 있어서 나온 것**이다. 발견 7 이 학습 곡선에 그대로
찍혀 있었는데, 곡선이 우상향이라 여태 못 봤다. → **학습 보상만 보고 판단하지 말 것.**

※ 이때 `episode_length` 도 같이 드러났다. `standup.py` 의 `config.episode_length = 500`
(10초)은 **학습에 안 쓰인다** — brax `ppo.train` 이 `brax_ppo_config` 의 1000 을 받아간다.
실제로는 20초로 돌고 있었다. 기립엔 넉넉한 쪽이라 급하진 않지만 두 값이 어긋나 있다.

※ 잡 **901992** 는 여태 이 파일 잡 표에 빠져 있었다. v4 가 거기서 나왔다.

---

## 발견 6 — 시작 자세가 커리큘럼이 아니었다

`standup.py` 의 reset 은 home 자세를 수평축 기준 20~180도로 기울여 z=0.22 에서 놓는다.
"각도를 넓게 뽑으면 쉬운 것부터 어려운 것까지 저절로 커리큘럼이 된다" 는 의도였다.
그 자세들을 **실제로 바닥에 가라앉혀 보니** 의도대로 안 되고 있었다.

| 기울임 | 가라앉은 뒤 | 바닥에 닿는 것 |
|---|---|---|
| 90° | up 0.00 · base 14.7cm | 몸통 |
| 120° | up 0.00 · base 14.7cm | 몸통 |
| 140° | up 0.00 · base 14.7cm | 몸통 |
| 160° | up -0.17 · base 16.2cm | 몸통 |
| **175~180°** | **up -0.99 · base 23.1cm** | **머리** |

**90도부터 160도까지가 전부 같은 자세다.** 소수점 둘째 자리까지 같다. 커리큘럼의 몸통 전체가
사실은 한 장의 카드였고, 정책은 같은 문제를 반복해서 받고 있었다.

그리고 **정확히 180도는 물구나무다.** 머리 충돌박스가 20x20cm 평판이라 (실측: 머리 메시
AABB 가 20.0 x 19.8 x 6.0 cm — 박스 근사가 틀린 게 아니라 머리가 진짜 넓적하다), 속도 0 으로
정확히 뒤집어 떨어뜨리면 그 위에 올라앉아 멈춘다. 발이 공중 35cm 에 뜬다. 초기 각속도를
3 rad/s 까지 줘봐도 안 빠져나온다.

→ 커리큘럼의 "제일 어려운 끝" 이 **현실에 없는 자세**였다. 넘어진 로봇이 아니라 물구나무다.
   `standup_feasibility.py` 를 그냥 돌리면 이 표를 맨 위에 찍어준다.

---

## 발견 7 — 누워 있는 동안에는 어떤 보상도 0 이었다

v3 의 보상표를 누운 자세(up = 0.00)에 대입해 보면:

| 항목 | 가중치 | 누웠을 때 값 |
|---|---|---|
| `upright` | +5.0 | 0.5 → **+2.5** |
| `height` | +3.0 | `× uprightness` = **0** |
| `feet_contact` | +1.5 | `× uprightness` = **0** |
| `attention` / `stillness` | +2.0 / +1.0 | `× settled` = **0** |
| `alive` | +0.5 | **+0.5** |

`uprightness = clip(up, 0, 1)` 이고 누운 자세는 up = 0 이다. 즉 **몸통이 수평인 동안은
`upright` 와 `alive` 말고 전부 정확히 0 이다.**

일어서기의 전반부 — 다리를 접어 당기고, 발을 바닥에 놓고, 몸통을 바닥에서 밀어내는 —
는 up 을 거의 안 올린다. 그 구간이 통째로 무보상이었다. 정책 입장에서는 몸부림쳐도
점수가 그대로이고 `action_rate` · `torques` 벌점만 붙으니, **가만히 누워 있는 게 최적**이다.
정책들이 실제로 그렇게 했다.

v1 의 `cost_stand_still` 함정(게이트가 늘 켜져 있어서 기립에 필요한 속도를 벌했다)과
09-04 의 `cost_head_pos` 함정(`move_cmd_norm` 게이트 때문에 제자리에선 꺼졌다)과
**같은 종류의 실수다. 이 레포에서 세 번째다** — 게이트를 달 때는 "그 게이트가 꺼져 있는
동안 학습이 뭘 보고 배우는가" 를 매번 확인할 것.

---

## 기립이 물리적으로 가능하긴 한가 — 열린 루프로 직접 찾아봤다

"남은 과제" 첫 줄에 1년 가까이 남아 있던 질문이다. **PPO 가 못 한 것과 로봇이 할 수 없는
것은 다른데, 실패한 학습 결과만 봐서는 구별이 안 된다.**

그래서 정책을 빼고 궤적을 직접 최적화했다 (`standup_feasibility.py`).
관절 목표값 waypoint 8개 → 선형보간 → 50Hz 서보. 제어 주파수·속도 제한·힘 한계는
학습 환경과 동일 (여기서 느슨하게 하면 "가능하다" 가 나와도 정책이 재현 못 한다).
탐색은 CEM, 개체군 256.

### 결과 1 — 열린 루프로 **웅크린 자세까지는 간다** (등을 대고 누운 상태에서)

마지막 waypoint 를 자유롭게 둔 탐색(192개 x 30세대, 108초):

| | 값 |
|---|---|
| 최고 점수 | 0.873 (재현 0.873 — 결정적) |
| 유지되는 자세 | up 약 0.85, 몸통 높이 약 10.6cm (서 있으면 15cm) |
| 성공 판정 | **미달** (up>0.9 이고 높이>12cm 를 1.5초 유지해야 함) |

즉 **누운 데서 웅크린 자세까지는 실제 서보 속도·토크 한계 안에서 올라오고, 그 자세를
2초 동안 유지한다.** 못 넘는 건 마지막 4~5cm 다.

### 결과 2 — 완전히 서는 건 열린 루프로 안 된다

마지막 waypoint 를 home(서 있는 자세)으로 고정하면:

| 탐색 | 최고 up | 끝에서 서 있는가 |
|---|---|---|
| home 고정 | **+0.97** | 아니오 (주 항 0.00) |
| home 고정 + **걷기 정책에 인계** (fr186 300M) | **+1.000** | 아니오 (256개 전부 실패) |

up 이 1.000 까지 찍히길래 처음엔 됐나 싶었는데, **필름스트립을 보니 1.7초에 수직을
스쳐 지나가고 그대로 무너진다.** 올라선 게 아니라 튕겨 넘어가는 중에 잠깐 수직이었던
것이다. 숫자만 봤으면 "성공했다" 고 썼을 자리다 — 궤적은 항상 그려서 볼 것.

인계 실험도 같은 결론이다. 걷기 정책(`fr186`)은 자기가 겪어본 적 없는 상태를 받으면
못 잡는다. 256개 후보 중 단 하나도 못 버텼다.

### 판정

**물리적으로 막혀 있는 게 아니다. 웅크리기까지는 열린 루프로도 되고, 남은 건 균형이다.**
그리고 균형은 고정된 궤적이 할 수 있는 일이 아니라 **피드백이 하는 일**이다 — 정확히
PPO 가 주는 것. **학습을 돌릴 가치가 있다.**

다만 "완전 전복(물구나무)에서 복구" 는 여전히 미검증이다. 이번 탐색은 **누운 자세**에서만
했다. 물구나무는 애초에 학습 시작 자세에서 거의 뺐으므로(7.4%) 급하지 않다.

### 덤: 재현성 버그를 하나 잡았다

워커가 `MjData` 를 재사용하는데 `qpos`/`qvel` 만 덮어쓰고 있었다. 솔버 warmstart
(`qacc_warmstart`)와 접촉 캐시가 이전 rollout 에서 남아, **같은 파라미터를 같은 워커에서
두 번 굴려도 점수가 달랐다** (0.095 vs 0.036). 탐색이 그 잡음을 신호로 착각하면 엘리트
선택이 통째로 망가진다. `mj_resetData` 를 먼저 부르는 것으로 해결. 지금은 재현값이 정확히 일치한다.


---

## 고친 것

### 1. 시작 자세를 미리 만들어 둔다 (`make_standup_poses.py`)

넘어지는 과정을 CPU 에서 미리 굴려 **바닥에 자리잡은 자세 512개**를 만들고
(`data/standup_poses.npy`, 43KB), reset 은 거기서 하나를 뽑는다.

- 기울임 축은 균일, 각도 90~175° (180°는 물구나무라 제외)
- 관절 목표값에 σ=0.25 rad 잡음 — 늘 home 모양으로 접힌 채 눕지 않게
- 떨어뜨릴 때 병진·회전 속도를 준다 (실제 넘어짐에는 속도가 있다)
- 정지 판정 통과분만 채택 (|v| < 0.05 m/s, |ω| < 0.3 rad/s)

결과 분포: 옆/등 69.9% · 엎드림 22.7% · 물구나무 7.4%, up -1.00 ~ +0.62.
**진짜로 누운 자세에서 시작하고**, 각도를 뽑던 때와 달리 분포가 실제로 넓다.

MJX 에서는 인덱싱 한 번이라 컴파일도 간단하다. 파일이 없으면 잡이 즉시 죽는다
(조용히 예전 reset 으로 되돌아가지 않도록 일부러).

### 2. `ground_clear` 보상 추가

몸통·머리 충돌박스의 **제일 낮은 모서리가 바닥에서 얼마나 떴는가** 를 0~5cm 로 재서
0~1 로 정규화. 가중치 2.0.

**누워 있는 동안 0 이 아닌 유일한 항목**이고, 이게 "가만히 누워 있기" 를 최적해에서
끌어내린다. 높이(`height`)와 달리 물구나무로는 못 딴다 — 머리가 닿아 있으니까.

⚠️ **처음엔 접촉 판정(`geoms_colliding`)으로 만들었다가 갈아엎었다.** 두 가지가 틀렸다:

1. `geoms_colliding` 은 **실제로 파고들어야** 참이 된다. margin 안쪽에 얹혀만 있으면
   거짓이다. 그래서 자세 뱅크의 **38%가 누워 있는데도 "떠 있음" 으로 읽혀** 보너스를
   그냥 받아갔다. (맨 MuJoCo 의 `d.ncon` 으로 세면 1.6% 라 안 보인다 — `ncon` 은 margin
   안쪽도 세기 때문. **두 API 가 접촉을 다르게 정의한다.**)
2. 0/1 이라 **몸을 4cm 드는 데 아무 보상이 없다.** 하필 정책이 찾아내야 하는 그 증분이다.

기하로 재면 둘 다 없다 — margin 과 무관하고 연속이다. 고친 뒤 누운 자세의 99%가
`ground_clear = 0`. 이건 `smoke_standup.py` 7단계가 잡아냈다 (아래).

무릎·정강이는 일부러 뺐다. **무릎 꿇기는 일어서는 과정의 정당한 중간 단계**라,
무릎 접촉을 벌하면 도달해야 할 자세를 벌하는 게 된다.

그리고 `feet_contact` 의 게이트를 `uprightness` 에서 `ground_clear` 로 옮겼다.
원래 게이트의 목적("등 대고 누워 발바닥만 바닥에 붙이는 꼼수 차단")은 `ground_clear`
쪽이 더 잘 막는다 (등을 대고 있으면 몸통이 닿아 있으니 0). 그러면서 발을 딛는 게
실제로 중요한 구간에서 항이 살아 있다. **발은 바닥에, 몸은 공중 = 무릎 꿇은 자세**,
거기서 한 번만 밀면 선다.

### 3. `smoke_standup.py` 에 "보상 사다리" 검사 추가 (7단계)

v1~v4 를 망친 건 **매번 같은 지점**이었다. 항목 하나하나는 말이 되는데 합쳐 놓으면
"가만히 있기" 가 제일 이득인 배치가 된다. 그래서 이제 클러스터에 올리기 전에
세 자세의 **총점**을 직접 찍어서 단조인지 본다.

```
누움(8회 평균)  총점  +2.967   ground_clear 이 0 인 비율 99%
몸통 들림        총점  +4.972          <- 차이 +2.004 = ground_clear 가중치
서 있음          총점 +15.000
```

"몸통 들림" 은 같은 자세에서 몸통만 12cm 올린 것이다. 관절 각도가 그대로라 up 은 안
변한다 — 즉 **"아직 수평인데 바닥에서 떨어졌다"**, 일어서기 전반부 그 자체다.
예전 보상표로는 이 차이가 0 이었다.

**이 검사가 실제로 내 실수를 두 번 잡았다.** 자세 하나만 뽑아 보던 첫 판(뱅크의 1.6%인
예외 자세를 뽑아 사다리가 거꾸로 보였다)과, 위의 접촉 판정 문제(38%가 새고 있었다).
자세 8개 평균으로 바꾸고 나서 통과.

### 4. `eval_standup.py` — 각도별 채점

앞으로 기립 정책은 뷰어보다 이걸 먼저 볼 것.

---

## 다음 잡 (학교 가면)

`ubai_standup/README.md` 에 scp 목록부터 채점까지 정리해뒀다. **`standup_poses.npy` 를
같이 보내야 한다** (서버 repo 는 upstream HEAD 기준이라 standup 커밋이 아예 없다).

```bash
# 스모크 2M
SEGMENT=2000000 TOTAL=2000000 OUTDIR=checkpoints_standup_smoke \
  sbatch --export=ALL,SEGMENT,TOTAL,OUTDIR $HOME/ubai_standup/30_train_standup.sbatch
# 본학습 300M (gpu1 68,700 step/s 기준 약 73분)
sbatch --export=ALL,SEGMENT=300000000,TOTAL=300000000,OUTDIR=checkpoints_standup_v5 \
  $HOME/ubai_standup/30_train_standup.sbatch
```

판정선: **90도 이상에서 성공률이 0 을 넘으면** 설계 수정이 먹힌 것이다.
45도까지만 되면 `ground_clear` 가중치를 올린다. 20도도 안 되면 회귀 — `standup_poses.npy`
가 서버에 갔는지부터 본다.

## 남은 과제 (갱신)

- [x] ~~**누운** 자세에서 기립이 물리적으로 가능한지~~ → 웅크리기까지는 된다 (위 참조)
- [ ] **물구나무**(완전 전복)에서 복구는 여전히 미검증. 이번 탐색은 누운 자세만 했다.
      학습 시작 자세에서 7.4% 로 줄여놨으므로 급하진 않다
- [ ] v5 학습 결과 채점 (`eval_standup.py`)
- [ ] 걷기 정책 ↔ 기립 정책 전환 로직. `--handoff` 로 구조는 이미 돌려봤는데
      **fr186 은 못 잡는다** (256개 전부 실패). 인계 시점 상태가 걷기 정책의
      학습 분포 밖이다 → 기립 정책이 스스로 안정화까지 하게 두거나,
      걷기 쪽에 "받아내기" 를 따로 학습시켜야 한다
- [ ] 기립 정책도 정격 토크(1.86 N·m)에서 되는지. 지금 씬은 3.23 이다
      (`standup_feasibility.py --force 1.86` 으로 궤적 쪽은 바로 잴 수 있다)

---

# 2026-09-16 — v5 도 실패했다. 보상 문제가 아니었다

## v5 결과: 자기 학습 분포에서 0/120

09-15 에 던진 300M (잡 984812) 은 COMPLETED, 5시간 34분, 최종 보상 91.85. **그런데 성공률은 0 이다.**

처음엔 각도 스윕으로 쟀는데 45도 이상 전부 0/8 이었다. 그런데 **그 측정 자체가 틀렸다** —
`eval_standup.py` 의 각도 스윕은 구버전 reset(공중 z=0.22 낙하)을 재현한 것이고, v5 는
바닥에 자리잡은 자세로 학습했다. 학습한 적 없는 분포로 시험한 셈이다.
→ `--bank` 모드를 추가해 **학습이 실제로 쓰는 시작 분포**로 다시 쟀다:

| 시작 자세 | 개수 | v5 최종 up | v4 최종 up |
|---|---|---|---|
| 옆/등 | 105 | +0.214 | +0.185 |
| 물구나무 | 9 | **+0.356** | -0.000 |
| 거의 엎드림 | 6 | +0.515 | +0.399 |
| **성공** | **0/120** | | 0/120 |

물구나무 탈출은 확실히 나아졌다 (v4 는 아예 못 나온다). 하지만 최종 up +0.21 은 여전히
몸통이 수평이다. 학습 보상 91.85 도 새 보상표 기준 누움(59)~서있음(300) 구간의 **13.5%** —
v2 의 12.5% 와 사실상 같다. **`ground_clear` 2점을 한 번도 못 받아가고 있다.**

## 발견 8 — 정답 경로는 좋아지기 전에 **나빠진다**

`standup_feasibility.py` 가 찾은 정답 궤적을 따라가며 재봤다:

```
시각    up      바닥에서 뜬 높이   ground_clear
0.0s  -0.00        -0.0cm          0.00
3.0s  +0.03        -0.0cm          0.00
3.6s  -0.70        -0.0cm          0.00   <- 일어서기 전에 더 뒤집힌다
4.2s  -0.56        -0.0cm          0.00
4.8s  +0.55        -0.0cm          0.00
6.0s  +0.84        -0.0cm          0.00
```

두 가지가 한꺼번에 나온다.

**(1) `ground_clear` 가 정답 궤적 전 구간에서 0 이다.** 마지막에 up +0.84 가 돼도 몸통 박스
뒷모서리가 바닥에 남아 있다 (=서는 게 아니라 앉는다). 부분 점수를 주라고 넣은 항이 정작
알려진 정답 위에서 한 번도 안 켜진다. **사다리가 아니라 맨 끝의 절벽이었다.**

**(2) 정답은 up 이 -0.70 까지 떨어졌다 올라온다.** `upright` 가중치 5.0 이 그 구간을 강하게
벌한다. **up 에 단조인 보상을 쓰는 한 PPO 는 이 경로를 못 찾는다.** 지역 최소값이 아니라
탐색의 문제다 — 4초짜리 협응 동작을 가우시안 액션 노이즈가 우연히 만들 확률은 0 이다.

→ **보상을 더 만지는 건 답이 아니다.** v1~v5 가 전부 보상 수정이었고 전부 같은 곳에서 멈췄다.

## 고친 것: 탐색을 건너뛴다 (RSI)

`make_standup_refstates.py` — 정답 궤적을 재생하며 제어스텝 5개마다 상태를 뽑아
`data/standup_refstates.npy` (60개, 각 행 `[qpos(21)|qvel(20)]`) 로 저장. qvel 이 반드시
필요하다 — 궤적 중간에는 운동량이 있고 0 으로 두면 존재하지 않는 상태가 된다.

`standup.py` 의 reset 이 `REF_FRACTION` 확률로 이 표에서 시작한다 (기본 0.5, 환경변수로
덮어쓸 수 있음). 정책은 **"거의 다 선 상태에서 마무리하기" 를 먼저 배우고**, 가치 함수가
그 지식을 뒤로(=up 이 꺼지는 어려운 구간으로) 전파한다. DeepMimic 의 reference state
initialization 과 같은 수법이고, 사람 모캡 대신 CEM 이 찾은 궤적을 쓴다.

나머지 절반은 자세 뱅크를 유지한다 — 레퍼런스만으로 학습하면 꼬리만 배우고 정작
채점받는 분포(누워서 시작)를 못 배운다.

## v6 스윕 (2026-09-16 제출)

스모크 985519 exit 0 (보상 38.3). 본학습 3개를 `REF_FRACTION` 만 바꿔 병렬로:

| 잡 | REF_FRACTION | 출력 |
|---|---|---|
| 985526 | 0.25 | `checkpoints_standup_v6_rf025` |
| 985527 | 0.50 | `checkpoints_standup_v6_rf050` |
| 985528 | 0.75 | `checkpoints_standup_v6_rf075` |

각 300M, 약 5.5시간 (자기충돌 때문에 14,400 step/s).

⚠️ **환경변수가 실제로 전달되는지 별도 잡(985529)으로 확인했다** — `shell=0.25 / parsed=0.25`.
어제 `SEED` 가 조용히 무시되던 걸 겪었으므로 이제 스윕 축은 반드시 먼저 검증한다.

## 채점

```bash
.venv\Scripts\python.exe eval_standup.py -o from_ubai\<파일>.onnx --bank 120
```

**각도 스윕(`--angles`)이 아니라 `--bank` 를 쓸 것.** 각도 스윕은 v2~v4 와 비교할 때만 쓴다
(그쪽이 구버전 reset 분포라서). 판정선: **0/120 을 넘으면** RSI 가 먹힌 것이다.

## v5 를 눈으로 본 것 (뷰어, 09-16)

`view_standup.py` 추가 — `mujoco_infer.py` 는 home(서 있는) 키프레임에서 시작하므로
기립 정책을 띄워도 일어서는 걸 볼 수가 없다. 이건 자세 뱅크에서 시작하고, 키로
레퍼런스 궤적 위를 시간순으로 걸어다닐 수 있다. 제어 루프는 `eval_standup.py` 의
`StandupEval` 을 그대로 import 해서 쓴다 (뷰어와 채점기가 다른 루프를 돌면 둘 다 못 믿는다).

### 실패 방식: 버둥거리는 게 아니라 **얼어붙는다**

```
[뱅크 0]   up -0.07 에서 시작
 4.1s  up +0.226  12.2cm
44.5s  up +0.230  12.1cm    <- 40초간 소수점 셋째 자리까지 그대로
```

시작이 달라도 몇 개의 고정점(up ≈ 0.00 / 0.23)으로 빨려 들어가 정지한다.
몸부림치다 실패하는 게 아니라 **지역 최소값에 못 박혀 있다.**

### 마무리조차 못 한다 (`eval_standup.py --ref`, 0/60)

정답 궤적 60개 지점 전부에서 시작시켜 8초 버티기:

| 궤적 시각 | 시작 up | 8초 뒤 up | 높이 |
|---|---|---|---|
| 5.0s | **+0.89** | +0.024 | 14.8cm |
| 5.1s | **+0.91** | +0.024 | 14.8cm |
| 5.7s | +0.84 | -0.000 | 14.7cm |

**거의 서 있는 상태에서 시작해도 1초 안에 무너진다** (뷰어에서 `up +0.84 -> -0.28` 확인).
주저앉는 게 아니라 **뒤로 넘어간다.** 수동적으로 넘어지는 것도 아니고, 어느 상태에서
시작하든 같은 고정점으로 **능동적으로 내려간다.** v5 가 배운 건 기립이 아니라
"눕는 자세로 수렴하기" 다.

⚠️ 단 이건 v5 가 저 상태들을 **학습에서 본 적이 없어서** 생기는 분포 밖 실패다
(RSI 는 v6 부터). 즉 v6 가 망한다는 뜻이 아니라, 정확히 RSI 가 고치려는 지점이다.
보상 크기로도 앞뒤가 맞는다 — 서 있으면 스텝당 15.0, 누우면 2.97 이다.

### 뷰어 버그 두 개 (둘 다 내가 심은 것)

1. **자세를 바꾸면 초당 출력이 통째로 사라졌다.** 경과시간은 0 으로 되돌리면서
   `last_report` 는 안 되돌려서, 이전 에피소드 값(예: 44.5초)까지 다시 자랄 때까지
   아무것도 안 찍혔다. T 를 20번 넘게 눌러도 로그가 비어 있었다.
2. **키를 연타하면 죽었다 (exit 139, 세그폴트).** 키 콜백은 뷰어 스레드에서 도는데
   거기서 qpos 를 쓰고 `mj_forward` 를 불렀다. 메인 루프의 `mj_step` 과 같은 MjData 를
   동시에 건드리니 경합이 난다. → 콜백은 요청만 큐에 넣고 적용은 메인 루프가 한다.

**MuJoCo passive viewer 의 `key_callback` 에서는 물리 상태를 건드리지 말 것.**

## v6 채점 결과 — RSI 는 안 먹혔다 (2026-09-22)

```bash
.venv\Scripts\python.exe eval_standup.py -o from_ubai\v6_rf025_300M.onnx \
    from_ubai\v6_rf050_300M.onnx from_ubai\v6_rf075_300M.onnx --bank 120
```

| 정책 | 자세뱅크 120 | 레퍼런스 유지 60 (`--ref`) | 수렴점 (최종 up / 높이) |
|---|---|---|---|
| rf025 | **0/120** | **0/60** | +0.216 / 11.3cm |
| rf050 | **0/120** | **0/60** | +0.225 / 11.1cm |
| rf075 | **0/120** | **0/60** | +0.220 / 11.1cm |

판정선은 "0/120 을 넘으면 RSI 가 먹힌 것" 이었다. 못 넘었다. 그리고 `REF_FRACTION` 을
0.25 → 0.75 로 세 배 갈랐는데 **세 결과가 구분이 안 된다.** 스윕 축이 병목이 아니다.

서버 `standup.py` 를 직접 열어 구현은 확인했다 — 102행에서 env 를 읽고, 287~295행에서
`use_ref` 를 추첨해 레퍼런스 qpos/qvel 을 실제로 쓴다. 코드 탓이 아니다.

실패 양상은 v5 와 같다. 레퍼런스 궤적 60개 지점 **전부**가 하나의 고정점으로 빨려
들어간다. up +0.91(거의 선 자세)에서 시작해도 8초 뒤 +0.029 다.

### ⚠️ 높이 기준이 아무 일도 안 하고 있다

`standup.py` 의 reset docstring 이 직접 쓰고 있듯, **넘어져 자리잡은 자세가 up 0.00 에
base 14.7cm** 다. 서 있을 때(home 15cm)와 사실상 같다. 그래서 `eval_standup.py` 의
`HEIGHT_OK = 0.12` 는 **누워 있어도 통과한다.** 표의 "최종 높이 11~14cm" 는 거의 선
것처럼 읽히지만 누워 있다는 뜻이고, 판정은 전적으로 `up > 0.90` 이 혼자 하고 있다.

**이 로봇에서 몸통 높이로는 선 것과 누운 것을 구별할 수 없다.** 진행 지표로 쓰지 말 것.

## 걷기 회귀 두 건 (2026-09-22)

`eval_walk.py` 신설. 뷰어 없이 명령 추종을 잰다. 평지 backlash, 12초, 머리는 정책이 구동.
**정책마다 학습 당시 명령 범위로 `ref_range` 를 맞춰서** 잰다 — 안 맞추면 범위 밖 입력의
실패를 정책 품질로 오독한다.

### 1. 제자리 옆걸음 상실 — 원인은 09-04 의 `lin_vel_y` 정합

각 정책을 자기 설정에서 잰 좌 게걸음 횡변위:

| 정책 | 날짜 | 학습 dy | 좌 게걸음 |
|---|---|---|---|
| 08-31 완성본 | 08-31 | ±0.2 | **+42.9cm** |
| head 910949 | 09-04 | ±0.2 | **+24.0cm** |
| fast 910953 | 09-04 | **±0.111** | +4.4cm |
| hw3 / hw8 | 09-06 | ±0.111 | +2.4 / +0.3cm |
| fr186 | 09-12 | ±0.111 | −0.0cm |

경계가 **910953 에 정확히 놓인다.** 910949 와 910953 은 둘 다 300M · 평지 ·
`HEAD_POS_W=-1.0` 이고 **속도범위 정합 하나만 다르다**(400~401행). 변수 1개 차이다.

`HEAD_POS_W` 가설은 틀렸다. hw3(−3.0) 과 hw8(−8.0) 이 똑같이 0 이다.

**다만 잃은 것은 게걸음 전체가 아니라 "제자리" 옆걸음이다.** 좁은 dy 정책도 걸음이
이미 돌고 있으면 옆으로 간다 — hw8 대각 전진+좌 횡 +112.8cm, 대각 전진+우 −10.1cm
(전진만 줬을 때 +43.2cm 기준). 정지 상태에서 옆으로 떼는 것만 못 한다.

메커니즘 추정: `reward_imitation` 내부 `w_joint_pos=15.0` 이 관절을 레퍼런스의
dy=0.111 사이드스텝에 강하게 묶는데, 그 레퍼런스 걸음 자체가 실제 횡변위를 거의
안 만든다. 예전엔 `tracking_lin_vel`(2.5) 이 "0.2 로 가" 라고 밀어서 그 차이가 실제
옆걸음을 만들었다. 둘을 같은 편으로 만들자 **미는 힘이 사라졌다.** 426행이 "충돌" 로
적은 것이 사실은 횡방향 추진력이었다.

⚠️ dx 정합(0.15→0.222)은 **되돌리지 말 것.** 레퍼런스가 원래 0.222 를 갖고 있어서
어느 쪽으로도 충돌이 없고, 전진이 78cm/8초 → 124~133cm 로 올랐다. 문제는 dy 뿐이다.

### 2. fr186 은 요 제어가 무너졌다 — dy 와 별개 문제

12초, 전진 명령만 줬을 때의 쏠림과, 제자리 좌회전 권한:

| 정책 | 직진 명령 시 쏠림 | 제자리 좌회전 |
|---|---|---|
| 08-31 완성본 | +20.2° (1.7°/s) | +592.9° (49°/s) |
| head 910949 | −2.2° (0.2°/s) | +691.5° (58°/s) |
| hw8 | +26.7° (2.2°/s) | +402.3° (34°/s) |
| **fr186** | **+104.6° (8.7°/s)** | **+59.6° (5°/s)** |

fr186 은 회전 권한이 다른 정책의 **7~12분의 1** 이고, **제 쏠림(8.7°/s)이 제 최대
회전 속도(5°/s)보다 빠르다.** 직진시키면 명령으로 되돌릴 수 있는 것보다 빠르게 왼쪽으로
휜다. 이동 거리로도 같은 말이 나온다 — 전진 91cm 에 횡 122cm 로, **옆으로 간 게 앞으로
간 것보다 많다.** 원을 그린 것이다.

같은 좁은 dy 인 hw8 이 멀쩡하므로 dy 정합과는 다른 원인이다. 아직 안 밝혔다.
차이는 `from_ubai/server_wip_2026_09_12/server_wip_20260912.patch` 에 들어 있다.

⚠️ **`from_ubai/fr186_*` 는 실기용에서 내려야 한다.** 55행이 실기용으로 지정해 뒀는데,
그 판단은 토크 감쇄 내성만 보고 내린 것이고 직진성·회전은 잰 적이 없었다.
지금 조종해서 볼 거면 `from_ubai/2026_08_31_144425_300482560.onnx` 를 쓸 것
(`--ref_range` 없이, 씬은 `scene_flat_terrain_backlash_collision.xml`).

### 균형은 이 문제의 축이 아니다

dy 를 ±0.2 로 되돌리면 충돌이 돌아오는데 그 대가로 균형을 잃는지 따로 쟀다.
정지/전진/게걸음/대각/회전/전진+회전/대각+회전 8조건 × 4정책 × 12초, **최저 up 이
0.987~0.999 로 전부 붙어 있고 아무도 안 넘어진다.** 넓은 dy 와 좁은 dy 가 구분이 안 된다.
되돌리기를 균형 걱정으로 막을 이유는 없다. 실제 비용은 걸음 품질 쪽이다.

단 되돌린 조합(dx 0.222 + dy ±0.2)은 **아직 아무도 학습해 본 적 없는 조합**이다.
08-31 은 dy 가 넓지만 dx 도 좁았다.

### 측정할 때 밟은 함정: 요를 시작·끝 차분으로 재면 안 된다

180도를 넘는 회전이 반대 부호로 접힌다. 08-31 의 제자리 좌회전을 처음에 **−127°**
(우회전) 로 읽었는데 실제로는 **+592.9°** (좌회전 1.6바퀴) 였다. `eval_walk.py` 는
매 제어스텝 차분을 누적한다. Q=좌회전 / E=우회전 은 뷰어에서 확인했다.
