# Open Duck Mini v2 — 하드웨어 준비

작성 2026-09-08. 출처: 업스트림 [BOM 스프레드시트](https://docs.google.com/spreadsheets/d/1gq4iWWHEJVgAA_eemkTEsshXqrYlFxXAPwO515KpCJc/edit),
`docs/assembly_guide.md`, `docs/configure_motors.md`, `Open_Duck_Mini_Runtime` README.
출력 목록은 [`print/PRINT_CHECKLIST.md`](print/PRINT_CHECKLIST.md) 참조.

---

## 0. 왜 지금 질러도 되는가

README 에는 "시뮬에서 걷는 걸 확인한 뒤 부품 주문" 이라고 써 뒀는데, **BOM 은 시뮬 결과와 무관하다.**
업스트림 설계(CAD·서보 배치·전원)가 고정이라 부품 목록은 이미 확정이고, 시뮬이 결정하는 건
"어떤 정책을 넣을 것인가" 지 "무엇을 살 것인가" 가 아니다.

해외 배송이 2~4주 걸리므로 **지금 발주하면 그 시간이 시뮬 마무리와 통째로 병렬로 흐른다.**
감수하는 리스크는 프로젝트를 접었을 때의 금액뿐이다.

---

## 1. 발주 웨이브 — 리드타임 긴 것부터

### Wave 1 — 오늘 발주 (해외, 2~4주)

| 품목 | 수량 | 비고 |
|---|---|---|
| **Feetech STS3215 7.4V (19kg·cm)** | **14** | ⚠️ **최장납기 + 최대금액. 이거 하나만이라도 오늘 지를 것** |
| 시리얼 버스 서보 어댑터 보드 | 1 | 아래 §3 주의 |
| BNO055 9-DOF IMU | 1 | |
| 마이크로 스위치 SS-10 | 4 | 발 접지 감지 |
| SG90 9g 서보 | 2 | 귀(안테나) 구동 |
| **626ZZ 베어링** | 3 (+예비, 10개입) | ⚠️ 608ZZ 아님 — §4 참조 |
| M3 열삽입 인서트 (M3×5×4 계열) | 100개입 | |
| M3 나사 세트 (6/8/10/12/16mm) | 세트 | 업스트림에 수량 명시 없음 |
| LED·아이 디퓨저·스피커·MAX98357A 앰프·리플렉터 | — | 표정 패키지. 나중에 해도 무방 |

### Wave 2 — 이번 주 (국내, 1~3일)

| 품목 | 수량 | 비고 |
|---|---|---|
| **18650 셀** | 2 | ⚠️ **항공 직구 사실상 불가 → 국내 필수.** 방전율 높은 것(30A급, 3000mAh) |
| 2S BMS / 18650 2구 홀더 | 1 / 1 | |
| 5V UBEC (레귤레이터) | 1 | Pi + SG90 + 로직용 |
| XT30 커넥터 (암수 1쌍) | 1 | |
| 소형 전원 스위치 / 2.1mm 배럴잭 | 1 / 2 | |
| USB-C 충전 모듈 | 1 | |
| **Raspberry Pi Zero 2W** | 1 | 국내 구매 권장(가품·핀헤더 이슈) |
| microSD 32GB+ | 1 | |
| micro-USB↔USB-C 케이블 15cm / 50cm | 1 / 1 | |
| 케이블 슬리브(사복관) | 1 | |

국내 조달처: 디바이스마트 · 엘레파츠 · IC뱅큐 · 메카솔루션.

### Wave 3 — 공구 (출력은 JLCPCB 외주 → §5)

| 품목 | 비고 |
|---|---|
| **인두 + 열삽입 인서트 팁** | 인서트가 수십 개라 팁 없으면 지옥 |
| **Loctite 243 (파란색)** | 금속↔금속 모터 나사 전용. 플라스틱 나사엔 쓰지 말 것 |
| **M3 리머 / 드릴비트 (3.0 · 4.0 · 4.2mm)** | ⚠️ 외주 출력은 재출력이 안 되므로 **구멍은 손으로 넓혀 맞춘다** |
| 버니어 캘리퍼스 | 도착 즉시 베어링 포켓·인서트 홀 실측용 |
| M3 육각/드라이버, 멀티미터, 니퍼·스트리퍼 | |

---

## 2. 업스트림 BOM 원본 (€, 업스트림 표기 그대로)

### 전원
| 부품 | 수량 | 단가 | 합계 | 비고 |
|---|---|---|---|---|
| 18650 셀 | 2 | 5 | 10 | "가장 빵빵한 걸로. 나는 3000mAh / 30A" |
| 18650 홀더 | 1 | 4.99 | 4.99 | |
| 2S BMS | 1 | 8.4 | 8.4 | |
| 5V 레귤레이터 | 1 | 4 | 4 | |
| 소형 전원 스위치 | 1 | 4.49 | 4.49 | |
| USB-C 충전기 | 1 | 9.99 | 9.99 | |
| 2.1mm 배럴잭 | 2 | 1 | 2 | |
| XT15/XT30 | 1 | 8 | 8 | 한 쌍만 필요하지만 낱개 구매가 어려움 |

### 서보 · 제어 · 센서
| 부품 | 수량 | 단가 | 합계 | 비고 |
|---|---|---|---|---|
| **Feetech STS3215 7.4V** | **14** | 14 | **196** | ⚠️ **"반드시 7.4V, 별칭 19kg.cm"** |
| IMU BNO055 | 1 | 40 | 40 | "더 싼 대안 필요. 알리에 1달러짜리 봄" |
| Raspberry Pi Zero 2W | 1 | 26.08 | 26.08 | |
| SD 카드 | 1 | 10 | 10 | |
| 서보 컨트롤 보드 | 1 | 5 | 5 | waveshare |
| 발 접점 스위치 SS-10 | 4 | 1 | 4 | |
| 9g 서보 | 2 | 3.33 | 6.66 | |

### 기타
| 부품 | 수량 | 단가 | 합계 |
|---|---|---|---|
| M3 열삽입 인서트 | 1세트 | 5.99 | 5.99 |
| PLA | 1 | 20 | 20 |
| TPU | 1 | ~0 | ~0 |
| 케이블 슬리브 | 1 | 9 | 9 |
| micro-USB↔USB-C 15cm / 50cm | 1 / 1 | 7 / 6 | 13 |
| **베어링** | **3** | 3.5 | 10.5 |

### 표정 패키지 (선택)
리플렉터 2 · 스피커 6 · LED 3개 0.6 · ~~마이크 15~~ · 아이 디퓨저 0.64 · 앰프 5 · Pi 카메라 5 (€)

> **마이크 제외 확정 (2026-09-10).** 런타임에 오디오 *입력* 코드가 없다 — §7 참조.
> 표정 패키지 34.24€ → **19.24€**. 소리는 **출력만** 한다 (§10.3).

> **업스트림 합계: 기본 398.1€ (최저가 조합 331.1€) / 표정 포함 432.34€.**
> 한국은 배송비·관부가세가 붙어 **55~65만원** 선 (기존 `NEXT_STEPS.md` 추정과 동일).

---

## 3. ⚠️ 돈·시간 날리는 함정

**서보 모델을 틀리는 것 — 제일 비싼 실수**
`STS3215` 는 7.4V / 19kg·cm 다. Waveshare 가 파는 `ST3215` 는 **12V / 30kg·cm 로 다른 물건**이다.
12V 를 사면 2S(7.4V) 전원 설계·BMS·UBEC 이 전부 안 맞고, 학습된 정책의 토크 가정(±3.23 N·m)도 어긋난다.
판매 페이지에 **"7.4V" 와 "19kg" 이 둘 다** 적혀 있는지 확인할 것.

**서보 ID 는 조립 전에, 하나씩**
`configure_motor.py --id <id>` 를 **모터 한 개씩 따로 연결해서** 돌려야 한다 (기본 ID 가 전부 같아서
버스에 여러 개 물리면 충돌). 조립 후에는 분해 없이 못 한다.

```
right: hip_yaw 10  hip_roll 11  hip_pitch 12  knee 13  ankle 14
left : hip_yaw 20  hip_roll 21  hip_pitch 22  knee 23  ankle 24
head : neck_pitch 30  head_pitch 31  head_yaw 32  head_roll 33
```

영점(zero position)도 이때 잡고 혼(horn)을 끼운다. 업스트림 曰 *"완벽하지 않아도 된다, 나중에 보정한다"*
(→ 런타임의 `find_soft_offsets.py`).

**서보 어댑터 보드는 FTDI 계열로**
런타임 설치 문서가 **udev 룰로 FTDI USB 시리얼 latency timer 를 낮추라고** 지시한다
(안 낮추면 제어 주기가 흔들림). 보드 모델명이 업스트림에 명시돼 있지 않으므로, 주문 전에
**칩이 FTDI(FT232) 인지** 확인하거나 Discord 에 확인할 것. 5달러짜리라 두 종류 사도 손해가 작다.

**18650 은 국내에서**
리튬 셀 낱개는 항공 특송 제한 품목이라 알리/아마존에서 개인 수입이 사실상 막힌다. 국내 구매가 정답.
업스트림 경고: **"홀더에 넣기 전 두 셀의 전압을 같게 맞춰 둘 것."**

**관세**
서보 14개만 해도 200달러쯤이라 개인 면세 한도(미화 150달러, 미국발 200달러)를 넘는다.
**개인통관고유부호** 준비하고, 관세·부가세 몫으로 10만원 안팎 여유를 잡아 둘 것.

**나사 고정제**
금속↔금속 모터 나사에는 Loctite 243(파랑). 진동으로 풀린다. **플라스틱 나사에는 절대 쓰지 말 것**
(케미컬 크랙).

---

## 4. 업스트림 문서의 구멍 — 직접 확인한 것

### 베어링: 608ZZ 가 아니라 **626ZZ(6×19×6)** 로 보인다

업스트림 BOM 은 "Bearing ×3" 이라고만 쓰고 규격을 안 적었고, `assembly_guide.md` 에도 모델명이 없다.
`NEXT_STEPS.md` 에 적어둔 **608ZZ(8×22×7)는 근거가 없고, STL 실측과 안 맞는다.**

`print/*.stl` 전체에서 원통 포켓을 실측한 결과:

| 파일 | 포켓 | 출력 수량 |
|---|---|---|
| `roll_motor_bottom.stl` | Ø**19.90** × 7.00 mm (관통) | ×2 |
| `head_yaw_to_roll.stl` | Ø**19.80** × 6.00 mm, 뒤에 Ø7.00 축 여유홀 | ×1 |

- Ø19.8~19.9 포켓이 **정확히 3개** → BOM 의 "Bearing ×3" 과 일치
- 바깥지름 **19mm**, 폭 **6mm**, 축 여유홀 Ø7 → 내경 6mm ⇒ **626ZZ (6×19×6)**
- Ø22 인 608ZZ 는 애초에 이 포켓에 안 들어간다

> 베어링은 1000원대라 **626ZZ 10개입**을 사고, 보험으로 6800ZZ(10×19×5) 몇 개를 같이 담아도 된다.
> 첫 판 출력 후 `roll_motor_bottom` 포켓을 캘리퍼스로 재보고 확정할 것.

### 안테나(귀)는 BOM 에도 print_guide 에도 없다

시뮬 메시 `antenna.stl` 실측: **길이 146mm, 뿌리 Ø1.8mm → 끝 Ø3.3mm, 끝에 Ø~4mm 구슬**, 부피 0.39cm³.
이 형상은 FDM 으로 세워 뽑는 게 사실상 불가능하다(1.8mm 굵기 146mm 기둥).

→ **Ø1.5~2mm 피아노선/스프링강선 또는 카본로드 150mm ×2 + 작은 비즈**로 대체하는 게 맞다.
`left/right_antenna_holder.stl` 이 SG90 에 물리는 홀더이므로 홀더 쪽만 프린트하면 된다. 비용 무시 가능.

### M3 나사 수량은 업스트림도 모른다

`assembly_guide.md` 원문에 `"X m3 screws (TODO : add the exact number)"` 로 남아 있다.
확정된 것은 트렁크 결합 M3×10 ×2, 발바닥 M3×6 ×2/발 뿐이고, 나머지는 **모터에 딸려오는 플라스틱 나사**를 쓴다.
→ **M3 6/8/10/12/16mm 혼합 세트**를 사 두는 게 유일한 답.

---

## 5. 프린팅은 발주와 동시에 시작

배송 2~4주 = 출력 20~35시간을 밀어 넣기에 충분하다. 순서 제안:

1. **테스트 판 먼저** — `roll_motor_bottom.stl` ×2 + `foot_bottom_pla.stl` ×2.
   1시간 이내에 끝나고, **베어링 포켓(Ø19.9)과 M3 인서트 홀을 실측**할 수 있다. 발주 확정 근거가 된다.
2. 큰 판 (head, body_*, trunk_*) — 밀폐 챔버면 상단/도어 개방 (PLA 열크립)
3. 다리·발 판
4. TPU 발바닥 ×2 — 따로 한 판, 40%, 저속, **AMS 말고 외부 스풀**

상세는 [`print/PRINT_CHECKLIST.md`](print/PRINT_CHECKLIST.md).

---

## 6. 부품 도착 후 순서

1. **서보 14개 ID·영점 설정** (조립 전! §3)
2. 열삽입 인서트 전부 심기 — 파트별로 몰아서 하는 게 빠르다
3. 발 → 다리 → 트렁크 → 목/머리 → 바디 (`assembly_guide.md` 순서)
4. 배선 — `docs/open_duck_mini_v2_wiring_diagram.png`
   ```
   좌/우 안테나 PWM : GPIO 12 / 13
   좌/우 발 스위치  : GPIO 22(핀15) / 27(핀13)
   IMU BNO055       : I2C (raspi-config 에서 I2C 활성화)
   오디오           : MAX98357A I2S
   ```
5. **Pi Zero 2W 셋업** — Raspberry Pi OS Lite (64-bit), 이미저에서 WiFi·SSH 미리 설정,
   `python3-pip python3-virtualenvwrapper`, FTDI latency udev 룰,
   `Open_Duck_Mini_Runtime` (v2) `pip install -e .`
6. `duck_config.json` 작성 + `find_soft_offsets.py` 로 관절 오프셋 보정
7. `BEST_WALK_ONNX_2.onnx` 로 첫 보행 시도 → **여기서부터 sim2real 튜닝 2~6주**

---

## 7. 미해결 / 확인 필요

- [ ] 서보 어댑터 보드 정확한 모델 (FTDI 여부) — 업스트림 미기재
- [ ] 베어링 626ZZ 확정 — 첫 출력물 실측으로 검증
- [ ] BNO055 정품 여부 — 알리 저가품은 클론(BNO055 아닌 MPU 계열)인 경우가 있음
- [ ] Xbox One 컨트롤러 — 런타임이 조이스틱 입력으로 씀. 있으면 유용
- [x] ~~마이크 €15~~ → **발주 제외 (2026-09-10).** 오디오 **입력** 코드가 하나도 없다.
      **사면 꽂을 데는 있어도 읽을 코드가 없다.** 소리는 출력만 (§10.3)
      - ⚠️ **근거 정정 (2026-09-10):** 처음 적었던 모듈 목록(`feetech*` / `hwi*` / `io_330` 포함)은
        **엉뚱한 브랜치**를 본 것이었다. 업스트림 기본 브랜치는 `main` 이 아니라 **`v2`** 다.
      - `v2` 브랜치 `mini_bdx_runtime/` 실제 전체: antennas · buttons · **camera** · duck_config ·
        eyes · feet_contacts · imu · onnx_infer · poly_reference_motion · projector · raw_imu ·
        rl_utils · rustypot_position_hwi · sounds · xbox_controller
      - 다시 세어도 **마이크·녹음·음성인식 0개** — 마이크 제외 결론은 그대로 유효하다
- [x] ~~Pi 카메라 €5~~ → **1차 발주 제외, 나중에 ESP32-S3 로 별도 추가 (2026-09-10). → §13**
- [x] ~~서보 토크 여유~~ → §12 에서 측정 완료. 발주 막지 않음. 단 `forcerange=1.86` 재학습 한 판이 남았다

---

## 8. JLCPCB(JLC3DP) 출력 발주 — 2026-09-08 조사

로컬 STL 36종 전부 부피·바운딩박스를 실측해서(`print/*.stl`) 검증한 결과.
**전체 solid 부피 1638 cm³** (non-TPU 1591 + TPU 47.2).

### 8.1 ⚠️ 함정 A — FDM 최소 크기 30×30×10mm 에 16종 28개가 걸린다

JLC3DP FDM 은 **바운딩박스 최소 30×30×10mm** 규정이 있다. 얇은 판재가 전부 탈락한다.

| 부품 | 수량 | 바운딩박스 (mm) | 걸리는 축 | 부피 합 |
|---|---|---|---|---|
| `head_bot_sheet` | 1 | 193.8 × 191.6 × **3.0** | 두께 | 65.9 cm³ |
| `knee_to_ankle_left_sheet` | 4 | 30.7 × **8.4** × 70.6 | 두께 | 33.0 |
| `foot_bottom_pla` | 2 | 93.0 × **27.7** × **8.0** | 폭·두께 | 31.8 |
| `leg_spacer` | 4 | **28.1** × 36.9 × 10.0 | 폭(1.9mm 부족) | 28.2 |
| `knee_to_ankle_right_sheet` | 4 | 30.7 × **7.2** × 70.6 | 두께 | 26.2 |
| `battery_pack_lid` | 1 | **7.5** × 51.4 × 86.4 | 두께 | 23.6 |
| `roll_motor_top` | 2 | 38.0 × **28.7** × 20.0 | 폭(1.3mm 부족) | 18.4 |
| `neck_left_sheet` | 1 | 30.7 × **7.1** × 58.0 | 두께 | 8.1 |
| `neck_right_sheet` | 1 | 30.7 × **5.8** × 58.0 | 두께 | 6.2 |
| `right_antenna_holder` | 1 | 26.9 × **20.0** × 41.7 | 폭 | 2.9 |
| `left_antenna_holder` | 1 | 26.9 × **20.0** × 41.7 | 폭 | 2.9 |
| `right_eye` | 1 | **8.4** × 40.0 × 40.0 | 두께 | 4.6 |
| `left_eye` | 1 | **4.3** × 35.0 × 35.0 | 두께 | 1.4 |
| `flash_reflector_interface` | 1 | **12.7** × **23.0** × **23.0** | 전부 | 2.2 |
| `bulb` | 1 | **2.0** × 35.0 × 35.0 | 두께 | 1.7 |
| **소계 (non-TPU)** | **26** | | | **257.1 cm³** |
| `foot_bottom_tpu` | 2 | 101.9 × 40.7 × **8.0** | 두께 | 47.2 (TPU) |

**→ 해결: NG 부품만 SLS 나일론 3201PA-F 로 발주한다.**
SLS/MJF 최소 크기는 **5×5×5mm** 라 전부 통과한다. 257 cm³ ≈ 257 g × **$0.18/g** ≈ **$46**.
PLA 보다 질겨서 `knee_to_ankle_*_sheet`(×8, 정강이 하중재) 에는 오히려 유리하다.

> **큰 부품을 SLS 로 돌리면 안 된다.** SLS 는 인필 없이 **속이 꽉 찬 채로** 나온다.
> 전체 1591 cm³ 를 나일론으로 뽑으면 플라스틱만 ~1.6 kg → sim 총질량 2.107 kg 을 통째로 깨뜨린다.
> 큰 셸은 반드시 FDM.

`foot_bottom_tpu` ×2 는 별도 처리: 두 장을 연결 포스트로 이어 붙여 Z 를 20mm 로 올리거나(니퍼로 절단),
8mm 고무/EVA 시트에서 잘라내거나, 국내 출력으로 돌린다.

### 8.2 ⚠️ 함정 B — JLC FDM PLA 는 인필 50% 고정 (업스트림 지정은 15%)

JLC3DP PLA-P 스펙: **기본 인필 50%**, 고객 선택 옵션 없음. 최대 250×250×300mm,
공차 ±0.3mm(100mm 이내), 권장 벽두께 >1.6mm, 색상 흑/백/적/청, 납기 72h~.

sim MJCF 총질량은 **2.107 kg** (`open_duck_mini_v2.xml`, 15개 바디 합).
인필이 3배가 되면 플라스틱이 무거워진다. 다만 영향은 부품마다 다르다:

- `head` — 부피 229 cm³ / 바운딩박스 2.34 L → **셸 구조(10%)**. 벽이 지배하므로 인필 영향 작음
- `leg_spacer` — 7.1 cm³ / 바운딩박스 10.4 cm³ → **68% 충실**. 인필 영향 큼

**예상 증가 +100~200 g** (2.107 kg 대비 5~10%).

### 8.2.1 결론: 인필 무게는 사실상 문제가 아니다

`runner.py:26` 이 `randomize.domain_randomize` 를 걸고 있고, 그 안에서:

| 랜덤화 | 범위 |
|---|---|
| 전 링크 질량 (바디별 **독립** 스케일) | ×U(0.9, **1.1**) |
| 트렁크 추가 질량 | +U(-0.1, +0.1) kg |
| 트렁크 CoM 지터 | ±0.05 m |
| dof armature | ×U(1.0, 1.05) |

**정책은 이미 바디별 ±10% 질량 변동 위에서 학습됐다.** 예상 증가 +5~10% 는 이 분포 안이다.
바디별 독립 스케일이라 "leg_spacer 만 무거워지는" 불균일 증가도 커버된다.

→ 그래도 **부품 도착 즉시 저울로 실측하고 MJCF `mass=` 를 갱신**한다. 공짜이고,
증가가 +10% 상단에 붙으면 학습 분포의 가장자리이므로 확인할 가치가 있다.
하지만 **이것 때문에 JLC 발주를 망설일 이유는 없다.**

### 8.3 발주 절차

1. [jlc3dp.com/3d-printing-quote](https://jlc3dp.com/3d-printing-quote) → Instant Quote
2. **파일당 모델 1개, 한 번에 10개까지 업로드** → 36종이므로 **4회로 나눠 올린다**
   (STL/STEP/OBJ/3MF 지원. ZIP 일괄 업로드는 안 됨)
3. 부품별로 프로세스·재질·색상·**수량** 지정 — 수량 1이 아닌 것은 `PRINT_CHECKLIST.md` §슬라이싱 절차 2번 목록 참조
4. 분배:
   - **FDM PLA** — §8.1 표에 없는 20종 (`head`, `body_*`, `trunk_*`, `foot_top`, `foot_side` 등)
   - **SLS 3201PA-F** — §8.1 표의 non-TPU 15종 26개
   - **FDM TPU** — `foot_bottom_tpu` ×2 (§8.1 단서대로 형상 보정 필요)
5. DFM 리뷰에서 반려되면 해당 부품만 SLS 로 옮긴다

**납기** FDM 72h~ + 국제배송 3~7일 ≈ **1.5~2주**. 서보 해외배송(2~4주)과 병렬로 흐른다.
**부대비용** 총 출력물 1~1.3kg → 배송비 + 관세. §3 의 개인통관고유부호 준비는 동일하게 적용.

### 8.4 JLC 로 안 넘어가는 것

색상 교체가 불가하므로 **판별 단색 전략(`PRINT_CHECKLIST.md` §주의)은 무의미**해진다.
대신 부품별로 색을 고를 수 있으니 머리/몸통만 다른 색으로 지정하면 된다.

---

## 9. 배터리 충전

업스트림 `assembly_guide.md` 는 **전원 계통을 문서화하지 않았다.** 사진 2장이 전부이고
유일한 문장이 *"홀더에 넣기 전 두 셀 전압을 맞춰라"* 다. BMS·레귤레이터·충전·XT30·배럴잭·스위치
지침이 전부 없다. 아래는 직접 정한 구성.

### 9.1 BMS 충전 말고 밸런스 충전기

BOM 의 "USB-C 충전기 €9.99" 는 2S 부스트 충전 모듈로 보이는데, **싼 2S BMS 는 밸런싱이 없는 경우가
많아** 셀 편차가 누적된다. 팩에서 **JST-XH 3핀 밸런스 리드**를 빼두고 RC 밸런스 충전기
(SkyRC B6 계열) 로 충전하는 쪽이 안전하고 검증돼 있다.

| 항목 | 값 |
|---|---|
| 모드 | LiPo(또는 Li-ion) **2S** |
| 만충 전압 | **8.4V** (4.2V/셀) |
| 충전 전류 | **1A** (3000mAh 기준 0.33C) |
| 방전 하한 | 셀당 **2.5V** 미만 금지 |
| 보행 지속시간 | **30~60분** 예상 |

### 9.2 배럴잭 2개의 용도

BOM 에 2.1mm 배럴잭이 **2개**인 이유: 하나는 충전 입력, **다른 하나는 벤치 전원**.
sim2real 튜닝은 수십~수백 회 반복이라 배터리로 돌리면 충전 대기에 시간을 다 쓴다.
**유선 급전 경로를 반드시 만들어 둘 것.**

### 9.3 셀 구매 시

- **버튼탑(button-top)** 으로 산다. 플랫탑은 2구 홀더 접점에 안 닿는다
- 홀더 삽입 전 두 셀 전압을 멀티미터로 맞춘다 (업스트림 유일 지침)
- 서보 구동 중 충전 금지

### 9.4 전압 확인

2S 만충 8.4V 는 STS3215 **7.4V** 사양의 상한(통상 6~8.4V) 안이다. 문제없음.
단 §3 의 12V `ST3215` 를 잘못 사면 이 전원 설계가 통째로 무너진다.

---

## 10. 조종 방법 — 혼자 걷지 않는다

정책은 **명령 7채널**을 입력으로 받는다 (`mujoco_infer.py:253-260`):

```
command[0..2] = lin_vel_x, lin_vel_y, ang_vel        # 이동
command[3..6] = neck_pitch, head_pitch, head_yaw, head_roll   # 머리
```

명령이 0이면 제자리에 서 있고, **속도를 넣어야 그 속도로 걷는다.** 자율보행이 아니다.

### 10.1 Xbox One 컨트롤러 (블루투스)

```
bluetoothctl
scan on
pair <MAC> / trust <MAC> / connect <MAC>
```

```
cd scripts/
python v2_rl_walk_mujoco.py --onnx_model_path <path>/BEST_WALK_ONNX_2.onnx
```

| 버튼 | 기능 |
|---|---|
| A | 보행 일시정지 / 재개 |
| LB | 스프린트 (누르는 동안 보행 주파수 ↑) |
| L/R 트리거 | 안테나(귀) |
| X / B | 프로젝터 / 랜덤 사운드 |
| **Y** | **머리 조종 — 업스트림 비권장 ("목이 부러질 수 있다")** |

### 10.2 Y 버튼 경고는 시뮬에서도 재현된다

`SIM_NOTES.md:447` 의 직접 측정과 일치한다:

| 명령 | 결과 |
|---|---|
| `neck_pitch` +1.1 (앞으로 63°) 유지 | 문제없음 |
| **`neck_pitch` -0.34 (뒤로 19°) 유지** | **넘어짐 (완전 전복)** |
| **`neck_pitch` + `head_pitch` 최대 숙임 동시** | **넘어짐** |

→ 실기에서도 머리 명령은 뒤로 젖히는 방향을 특히 조심한다.

### 10.3 사운드 — wav 재생이 전부다 (말은 못 한다)

TTS 도 음성합성도 없다. **미리 만든 `.wav` 를 트는 것뿐**이다.

| 항목 | 내용 |
|---|---|
| 재생 코드 | `scripts/sound_test.py` 하나. `pygame.mixer` 로 **현재 폴더의 `*.wav`** 를 찾아 재생 |
| 경로 | MAX98357A I2S 앰프 → 스피커 (배선 §6, 출력 STL `speaker_stand` / `speaker_interface`) |
| 조작 | 컨트롤러 **B = 랜덤 사운드** |
| ⚠️ | **업스트림 레포에 wav 파일이 없다.** 소리는 직접 준비해서 넣는다 |

뒤집어 말하면 **원하는 소리를 넣는 데 코드 수정이 필요 없다.** 대사를 TTS 로 미리 wav 로 뽑아
넣으면 마이크·네트워크 없이도 "말하는" 것처럼 보인다 — 보행 스택은 건드리지 않는다.

**필요 부품은 스피커 €6 + MAX98357A 앰프 €5 = €11 뿐**이다. 단 `speaker_stand.stl` /
`speaker_interface.stl` 은 몸통 내부라 **조립 전에 뽑아둔다** (`print/PRINT_CHECKLIST.md`).

---

## 11. 조립 가이드 웹페이지에 대한 평가

`robotics.growbotics.ai` 의 Open Duck Mini V2 조립 페이지는 **업스트림 GitHub 을 자동 생성한
파생 문서**다. 근거:

- 섹션 제목이 내용과 어긋난다 — "섹션 2: 3D 프린팅" 의 내용은 발/정강이/허벅지/힙 조립,
  "섹션 6: 머리 및 액세서리" 의 내용은 Raspberry Pi OS 플래시
- 20개 스텝이 전부 "5 minutes", 총 조립시간 "1시간 40분" → 플레이스홀더

**목차로는 유효하다.** 20스텝 구조가 §6 순서와 일치하고 서보 ID 배정도 맞다.
다만 **이 페이지만으로는 하드웨어가 완성되지 않는다** — 전원 계통(§9), 나사 수량(§4),
베어링 규격(§4) 이 원문 자체에 없기 때문이다. 원문 `docs/assembly_guide.md` 와 이 문서를 함께 본다.

---

## 12. ✅ 서보 토크 여유 — 측정 완료 (2026-09-09)

**sim 액추에이터 힘 제한과 STS3215 실제 정격이 안 맞는다.**

| 출처 | 값 | 환산 |
|---|---|---|
| `open_duck_mini_v2.xml:49` | `forcerange="-3.23 3.23"` N·m | **32.9 kg·cm** |
| `joints_properties.xml` (sts3215 클래스 기본) | `forcerange="-3.35 3.35"` N·m | 34.2 kg·cm |
| **STS3215 7.4V 실물 정격** | **1.86 N·m** | **19 kg·cm** |

sim 이 실제 서보의 **약 1.7배** 토크를 정책에 허용하고 있다. 3.23 N·m ≈ 33 kg·cm 는
오히려 12V `ST3215`(30 kg·cm) 쪽에 가까운 수치다 (§3 의 모델 혼동 함정과 같은 뿌리일 수 있음).

`SIM_NOTES.md:395` 에서 확인한 건 **목 서보의 정적 부하**(2.1 kg·cm, 여유 9배)뿐이다.
**보행 중 다리 서보의 피크 토크는 아직 아무도 안 쟀다.**

### ✅ 측정 완료 (2026-09-09) — 결론: **발주 막지 않는다**

스크립트 `ubai/torque_probe.py` (토크 통계) + `ubai/torque_derate_test.py` (감쇄 실험).
둘 다 `head_probe2.py` 방식 — 뷰어만 가짜로 갈아끼우고 제어 루프는 원본 그대로.
토크 피크는 제어주기(50Hz)가 아니라 물리 스텝에서 나므로 `mj_step` 을 감싸 500Hz 로 기록했다.

#### 1. sim 은 정격을 실제로 넘게 쓰고 있다 — 특히 무릎

정지·전진·후진·게걸음·좌우회전·전진+회전 8구간 × 5초, 구간별 앞 1초는 과도구간이라 버림.

| 정책 | 다리 피크 | 정격(1.86) 초과 시간 | 최악 관절 | 그 관절 클램프(3.23) 시간 |
|---|---|---|---|---|
| BEST_WALK_ONNX_2 | 3.23 N·m (32.9 kg·cm) | 10.8% | `right_knee` | **16.4%** |
| hw8 931375 | 3.23 N·m (32.9 kg·cm) | 8.7% | `right_knee` | **14.2%** |

정지 상태는 최대 1.85 N·m 로 정격 안이다. 문제는 걸을 때다.
**19 kg·cm 는 STS3215 의 스톨 토크** — 잠깐 넘는 게 가능한 정격이 아니라 물리적 상한이다.
즉 sim 의 무릎은 실물이 낼 수 없는 토크를 전체 시간의 14~16% 동안 쓰고 있다.

#### 2. 그런데 정격으로 깎아도 **안 넘어진다**

통계만으로는 판정이 안 된다 — 정책이 내는 건 토크가 아니라 **위치 목표**고, 실물 서보는
못 내는 토크에서 넘어지는 게 아니라 추종이 나빠질 뿐이다. 그래서 `model.actuator_forcerange`
만 바꿔서 직접 걸려 봤다 (30초: 정지3 / 전진15 / 좌회전6 / 전진6).

| forcerange | BEST_WALK 전진 (명령 0.150) | hw8 전진 (명령 0.222) |
|---|---|---|
| 3.23 (원본) | 0.135 m/s | 0.181 m/s |
| 2.80 | 0.131 | 0.177 |
| 2.40 | 0.117 | 0.129 |
| 2.10 | 0.099 | 0.081 |
| **1.86 (실물 정격)** | **0.078 (−42%)** | **0.057 (−69%)** |
| 1.60 | 0.040 | 0.035 |

**전 구간에서 한 번도 안 넘어졌다** (최저 up-vector 0.98, 학습 종료 기준은 up < 0). 회전 포함.

#### 3. 판정

- **발주 진행.** 리스크는 "안 걷는다" 가 아니라 "sim 보다 느리게 걷는다" 다.
  하드웨어 선택(STS3215 7.4V)이 틀린 게 아니라, sim 의 `forcerange` 가 12V `ST3215`(30 kg·cm)
  쪽 수치로 들어가 있는 것이다 — §3 의 모델 혼동 함정과 같은 뿌리다.
- **고치는 법은 재학습 한 판.** `forcerange` 를 1.86 으로 낮춰 300M. A6000 실측 1시간 22분이니
  다음 학교 방문 때 `alive` 스윕 회수하면서 같이 던지면 된다.
- ⚠️ **레퍼런스 정합 범위(`--ref_range`) 정책이 토크를 더 먹는다.** hw8 은 명령이 빠른 만큼
  (0.222 vs 0.150) 감쇄에 더 크게 무너진다 (−69% vs −42%). 빠른 명령 범위는 공짜가 아니다.
- 남은 미확인: 실물 서보는 **토크-속도 곡선**이 있어서 속도가 붙으면 스톨 토크보다 덜 낸다.
  여기 감쇄 실험은 속도와 무관한 상수 상한이라 **실물보다 관대하다.** 그래도 방향은 같다.

---

## 13. 카메라 / 비전 — "나를 따라다니는 로봇" (2026-09-10)

**최종 목표는 "나를 따라다니는 로봇" 이다.** (2026-09-10 사용자 확정)
**시간이 없다는 전제**(논문·유학준비 병행)에서 내린 결론:

> **된다. 그리고 머리 추종 문제와 상관없다.**
> **몸에 아무것도 안 찬다.** ESP32 가 영상을 쏘고 **노트북이 사람을 검출**해 Pi 로 명령을 보낸다.
> 카메라는 1차 발주에 넣지 않는다 — 나중에 붙여도 보행 스택을 한 줄도 안 건드린다.

### 13.0 ⚠️ "쳐다보기" 와 "따라다니기" 는 다른 문제다

처음에 이 둘을 묶어서 "머리 추종이 블로커"라고 적었는데 **틀렸다.**

| | 쓰는 명령 | 상태 |
|---|---|---|
| 사람 **쳐다보기** | `last_commands[3:7]` (머리 4축) | ❌ 막힘 — 정책이 16% 만 따라감 (§13.2) |
| 사람 **따라다니기** | `last_commands[0:3]` (몸통 vx,vy,wz) | ✅ **안 막힘** — 보행 명령 추종은 잘 된다 |

따라다니는 건 **몸통이 걸어가는 것**이므로 공 따라가기와 구조가 완전히 같다.
**목표에 머리를 쓰지 않으므로 `ALIVE_W` 스윕 결과를 기다릴 필요가 없다.**

### 13.1 시간이 실제로 어디로 가는가

| 단계 | 예상 | 불확실성 | 비고 |
|---|---|---|---|
| sim2real 보행 튜닝 | 2~6주 | 높음 | 피할 수 없음 (§6) |
| **머리 추종 16% 해결** | **미정** | **매우 높음** | `alive` 스윕으로 풀릴지 불명 |
| 공 따라가기 (배선+코드) | 2~3일 | 낮음 | 경로가 전부 확정돼 있음 |

**카메라는 시간이 가는 곳이 아니다. 위험한 건 머리 추종이다.**

### 13.2 왜 사람 쳐다보기를 빼는가

`SIM_NOTES.md` 기준 현재 상태 — 정책이 머리 명령의 **16%** 만 따라간다:

| `head_pos` | ±1.5 명령 → 실제 | 스윙 |
|---|---|---|
| -1.0 | ±0.21 (14%) | 0.423 |
| -8.0 | ±0.24 (16%) | 0.483 |

가중치를 8배 올려도 스윙은 0.42→0.48. **가중치로 풀 문제가 아니라고 이미 결론 났다.**
가설은 "머리를 크게 돌리면 넘어질 위험 ↑ → `alive` 를 잃는 쪽이 더 손해". 그래서
`ALIVE_W` 스윕(948600~602)이 도는 중인데, **실패하면 다음은 커리큘럼 설계**다 — 기한을 못 박는다.

카메라가 "사람이 왼쪽 30° 에 있다"고 완벽히 알려줘도 **로봇은 5° 만 돌린다.**
→ 사람 쳐다보기의 블로커는 카메라가 아니라 **정책**이다. 카메라를 먼저 사도 소용없다.

### 13.2.5 검출은 **노트북**이 한다 — 마커 없이 사람을 본다

사용자 요구: **몸에 뭘 차지 않는다. 로봇이 나를 보고 따라와야 한다.** (2026-09-10)

#### 카메라 기하 — 얼굴은 못 본다, 몸통은 본다

로봇 키 42cm, 몸통 카메라 높이 **~30cm**, OV2640 표준 렌즈 수직 화각 **약 ±21°**.
카메라를 **위로 20° 기울여** 다는 것을 전제로:

| 거리 | 프레임에 들어오는 높이 | 보이는 것 |
|---|---|---|
| 1.5m | 26 ~ 144cm | 허벅지~가슴 |
| 3m | 21 ~ 257cm | **전신** |

→ **얼굴 검출은 포기한다** (가까이서 얼굴은 프레임 밖). 대신 **몸통·다리를 본다.**
사람 검출기와 자세 추정기는 **부분적인 몸(다리만)에도 반응**하므로 이걸로 충분하다.
⚠️ **카메라 브래킷은 반드시 위로 ~20° 기울일 것.** 수평이면 1.5m 에서 무릎 아래만 보인다.

#### 검출 위치: ESP32 도 Pi 도 아닌 **노트북**

| 후보 | 판정 |
|---|---|
| ESP32-S3 온보드 | ❌ `esp-dl` 은 **얼굴** 검출 위주. 위 기하 때문에 얼굴은 안 보인다 |
| Pi Zero 2W 온보드 | ❌ 512MB + 50Hz 제어 루프와 CPU 경합 |
| **노트북 (채택)** | ✅ **ESP32 가 MJPEG 송출 → 노트북이 MediaPipe/YOLO 로 검출 → Pi 로 명령** |

**이 방식의 결정적 장점: 로봇이 없어도 지금 집에서 전부 만들 수 있다.**
노트북 웹캠으로 검출 → 7벡터 커맨드 출력까지 완성해두면, 로봇이 왔을 때 입력을 웹캠에서
ESP32 스트림으로 바꾸는 것만 남는다.

**트레이드오프:** 노트북이 같은 WiFi 에 켜져 있어야 한다. 이게 싫어지면 그때
컴퓨트 보드를 **Radxa Zero 3W**(RK3566, Pi Zero 와 65×30mm 동일 폼팩터, NPU 0.8 TOPS)로
바꿔 온보드로 옮긴다 — **지금 결정할 필요 없다.**

#### 거리 유지 / 한계

- 거리는 검출 박스 **높이**로 추정한다 — 크면 정지, 작으면 전진. 부정확하면 VL53L0X ToF(~$3) 추가
- ⚠️ **"나만" 알아보는 것(re-ID)은 다른 문제다.** 여러 사람이 있으면 "가장 가까운/큰 사람"을
  따라간다. 진짜 개인 식별이 필요해지면 그때 별도로 다룬다
- 마커(형광밴드·ArUco)는 **폐기가 아니라 폴백**이다. 조명이 나쁘거나 검출이 흔들릴 때
  디버깅용으로 쓸 수 있게 색 블롭 경로도 코드에 남겨둔다

### 13.3 이 경로가 안전한 이유

- **머리를 안 쓴다.** 몸통 속도 명령으로 몸째 돌면 된다. 보행 명령 추종은 이미 잘 되므로
  **지금 정책 그대로 동작한다** → 머리 추종(§13.2)이 안 풀려도 목표 달성에 지장 없다
- **학습이 없다.** MediaPipe/YOLO 기성 모델을 그대로 쓴다. 직접 학습시킬 것이 없다
- **붙는 지점이 확정돼 있다** (§13.4)

### 13.4 붙는 지점 — 보행 스택을 안 건드린다

`scripts/v2_rl_walk_mujoco.py` (업스트림 `v2` 브랜치):

```python
self.last_commands = [0.0] * 7
self.last_commands, self.buttons, lt, rt = self.xbox_controller.get_last_command()
head_motor_targets = self.last_commands[3:] + self.motor_targets[5:9]
```

| 인덱스 | 의미 | 공 따라가기 |
|---|---|---|
| `[0:3]` | 몸통 속도 vx, vy, wz | **여기만 쓴다** (따라다니기) |
| `[3:7]` | 머리 4축 목표 | 안 씀 (0 유지) — 쳐다보기를 포기했으므로 |

`XBoxController` 는 pygame 워커 스레드 → `Queue` → `get_last_command()` 구조다.
**같은 시그니처 클래스 하나를 만들어 갈아끼우면 끝.** 정책·서보 드라이버는 손대지 않는다.

### 13.5 하드웨어 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 모듈 | **XIAO ESP32S3 Sense** (~$14, 21×17.5mm) | AI-Thinker ESP32-CAM(27×40.5mm)은 LDO 빈약 → 브라운아웃 악명. `esp-dl` 얼굴검출도 S3 벡터명령이 있어야 쓸 만하다 |
| 위치 | **머리 말고 몸통 앞** | 출력 36종에 카메라 마운트가 **없다**(§13.6). 몸통이면 STL 수정 회피 + 영상 흔들림 감소 |
| 전원 | **별도 5V 벅** 또는 최소 1000µF 벌크 캡 | WiFi TX 때 300mA 스파이크. Pi 5V 레일과 공유하면 Pi 리셋 — "가끔 죽는" 디버깅 지옥 |
| Pi 로 보내는 것 | `(x, y, 크기)` 몇 바이트 | 검출을 ESP 가 하므로 **영상 전송 자체가 없다** — 대역폭·지연 문제 소멸 |

OV2640 은 센서가 JPEG 를 직접 뱉으므로 ESP 가 인코딩할 필요가 없다. 색 블롭이면 QVGA 30fps 여유.

⚠️ 걸을 때 머리 진폭이 `head_pitch` 0.266 / `head_yaw` 0.248 로 흔들린다(-8.0 에서 오히려 증가).
카메라를 몸통에 두는 이유가 이것이기도 하다. 그래도 방위각은 **저역통과 필터를 한 번 걸어서** 쓴다.

### 13.6 출력 부품에 카메라 자리가 없다

`print_guide_original.md` 36종에 카메라 마운트가 **아예 없다.**
눈(`left_eye`/`right_eye`)은 LED, `bulb`/`flash_light_module`/`flash_reflector_interface` 는
프로젝터(런타임 `projector.py` 가 GPIO D25 로 켠다), `left_cache`/`right_cache` 는 덮개다.
안테나(§4)와 같은 상황 — **직접 만들어야 한다.** 그래서 몸통 외부 브래킷이 제일 싸게 먹힌다.

### 13.7 업스트림 카메라 코드는 "없는" 게 아니라 "안 붙어 있다"

§7 에 처음 적은 "카메라 모듈이 없다"는 **틀렸다** — 엉뚱한 브랜치를 봤다.
기본 브랜치 `v2` 에는 `mini_bdx_runtime/camera.py` 가 있다. 다만:

- `picamzero` 로 **한 장** 찍고 → 512×512 → `/home/bdxv2/aze.jpg` 저장 → base64 인코딩
- 루프도 검출도 없다. VLM API 에 사진 한 장 올리는 습작 코드다 (`aze` = azerty 자판 뭉갠 것)
- **메인 런타임이 `camera.py` 를 import 하지 않는다** → 죽은 코드

결론("사도 바로 못 쓴다")은 같고 근거만 바뀐다: "코드가 없다" → **"연결이 안 돼 있다"**.
반대로 `projector.py` 는 import 된다.

### 13.8 순서 — 로봇 없이 미리 할 수 있는 것부터

1. **지금 (로봇 없이, 집에서)** — 노트북 웹캠으로 사람 검출 → 커맨드 7벡터 출력까지 만들어 둔다.
   로봇이 없어도 검증이 끝나고, 나중에 남는 건 플러밍뿐. **집에서 할 수 있는 작업이다**
   (UBAI 는 학교에서만 접속되므로 집 시간을 여기에 쓴다)
2. 부품 도착 → 조립 → **실물 보행 (sim2real 2~6주)** ← 여기가 진짜 일
3. XIAO 를 몸통에 브래킷으로 달고 `(x,y,크기)` 를 Pi 로 → 1번 코드에 연결
4. (선택) 노트북을 떼고 싶으면 Radxa Zero 3W 로 온보드 이전. 머리 추종이 풀리면 "쳐다보면서 따라오기\"

### 13.9 발주 타이밍

XIAO ESP32S3 Sense 는 소액(~$14)이라 **Wave 1 에 끼워 넣어도 배송이 안 바뀐다.**
국내 유통이 있으면 Wave 2 로 미뤄도 무방. 어느 쪽이든 **일정 위험이 없다** — 그래서
"지금 결정하지 않는다"가 비용 0 인 선택지다.
